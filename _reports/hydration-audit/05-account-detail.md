# 05 — account-detail (`/accounts/[id]`) — audit d'hydratation

**Verdict global : H5 (cassé).** The account-detail page is largely high-fidelity: header, targeting/suppression, AI brief, AI summary, meeting/call intel, deals, score, and the editable right panel are all wired to real tenant-and-company-scoped data with their own loading/empty/error degradation. The one real data defect is the "Contacts at this account" lane: the page fetches /api/contacts?companyId=<id> but that GET handler never reads the companyId param, so it returns the first 50 of ALL tenant contacts instead of contacts at this account — and the correctly-scoped contacts already present in the main account payload are discarded. The dossier lane is H2 because a 500 from buildDossier is silently rendered as "no dossier / generate" rather than an error.

Entrée : `app/apps/web/src/app/(dashboard)/accounts/[id]/page.tsx`.

## Éléments

| Élément | file:line | Source (file:line) | État | Tenant | Loading | Empty | Error | Fresh | Note |
|---------|-----------|--------------------|------|--------|---------|-------|-------|-------|------|
| Header: account name / domain / industry | page.tsx:336-339 | GET /api/accounts/[id] -> companies row, tenant+id scoped (api/accounts/[id]/route.ts:21-31,167-180) | H1 | yes | skeleton | none | global | once | Real tenant-scoped firmographics; DetailPageSkeleton while loading; 'Account not found' on failure (page.tsx:315-316). Faithful. |
| Header avatar initial | page.tsx:332-334 | account.name.charAt(0) (page.tsx:318) | H1 | yes | skeleton | n/a | global | once | Derived from real account name. Faithful. |
| Owner select | page.tsx:343 | account.ownerId from GET /api/accounts/[id]; PUT to same route on change (route.ts:227,235-241) | H1 | yes | skeleton | blank | silent | once | Real ownerId, optimistic PUT. reassignAccountOwner swallows errors (page.tsx:128-130) — minor, but value is real. |
| Targeting status badge | _targeting-suppression-panel.tsx:123-128 | account.targetingStatus from GET /api/accounts/[id] (route.ts:179) | H1 | yes | skeleton | blank | global | once | Real spec-35 targeting_status; defaults to 'Unreviewed' label when null. Faithful. |
| Suppression badges (DNC list) | _targeting-suppression-panel.tsx:130-157 | account.suppressions: suppression table, level/value-keyed, tenant-or-global, active only (route.ts:140-164) | H1 | yes | skeleton | blank | global | once | Real active suppressions; empty -> 'Do not contact' CTA. reloadAccount refetches after mutate (page.tsx:111-118). Faithful. |
| AI Intelligence Brief (brief + keyRelationships + suggestedAction) | intelligence-brief.tsx:80-100 | GET /api/accounts/[id]/intelligence -> LLM over tenant-scoped activities/contacts/deals/graph (intelligence/route.ts:54-160) | H1 | yes | skeleton | blank | independent | once | Own fetch+skeleton; written 'Not enough data' empty state (intelligence-brief.tsx:50-66) and a degraded 'Unable to generate' fallback (route.ts:163-170). 1h in-memory cache, tenant-keyed. Faithful. |
| AI Account Summary + How they make money | page.tsx:374-426 | account.properties.ai_account_summary / ai_how_they_make_money; refresh = POST /api/accounts/[id]/generate-summary (generate-summary/route.ts) | H1 | yes | spinner | none | independent | once | Real tenant data from properties; refresh has spinner+toast (page.tsx:392-405). Whole block self-hides when both empty (page.tsx:374) — acceptable self-hide. Faithful. |
| Meeting Intelligence card (teamSize/budget/tools/competitors) | page.tsx:429-481 | account.properties.meetingIntel (post-call extraction, persisted on company row) | H1 | yes | skeleton | none | global | once | Real persisted extraction; self-hides when <=2 keys (page.tsx:432). Each cell guarded by != null. Faithful. |
| Account Call Intel (stack / triggers + approve/dismiss) | page.tsx:484 / call-intel.tsx | account.properties.callIntel; review POST /api/call-intel/review (call-intel.tsx:77-81) | H1 | yes | skeleton | none | independent | once | Self-extracts from properties, returns null when nothing live/pending; human-in-the-loop review bar. Faithful. |
| Research Dossier (leadership/funding/techStack/ICP/approach/sources) | company-dossier.tsx:268-571 | GET /api/research/dossier?company=<domain> -> buildDossier(tenant-scoped) (research/dossier/route.ts:11-33) | H2 | yes | spinner | blank | independent | once | Real tenant-scoped dossier with own loading/no-domain/no-dossier/error states. H2: GET buildDossier can 500 (route.ts:27-31) and the component treats any non-ok as 'no dossier' -> shows the Generate CTA instead of an error, masking a server failure (company-dossier.tsx:129-131). |
| Contacts at this account (list + count) | page.tsx:504-528 | GET /api/contacts?companyId=<id> — but the GET handler NEVER reads companyId (contacts/route.ts:29-52, no searchParams.get('companyId')) | H5 | yes | none | blank | silent | once | DEFECT: page fetches /api/contacts?companyId=accountId (page.tsx:90) but the route ignores companyId entirely — returns the FIRST 50 of ALL tenant contacts. 'Contacts (N)' and the rows under 'Contacts at this account' show the wrong set (account-agnostic). The correctly-scoped contacts ARE already in the GET /api/accounts/[id] payload (route.ts:54-69) but are discarded. No loading state on this lane; errors only console.warn (page.tsx:93). |
| Opportunities / Deals (list + count) | page.tsx:531-556 | data.deals from GET /api/accounts/[id] -> deals scoped by companyId+tenantId (route.ts:38-53) | H1 | yes | skeleton | blank | global | once | Real company+tenant-scoped deals; written empty state 'No deals linked to this account.' Faithful. |
| About / description | page.tsx:496-501 | account.description from GET /api/accounts/[id] (route.ts:173) | H1 | yes | skeleton | none | global | once | Real data; self-hides when null. Faithful. |
| Suggested Contacts | page.tsx:653-715 | GET /api/accounts/[id]/suggested-contacts -> Apollo searchPeople by company.domain + ICP targeting (suggested-contacts/route.ts:41-65) | H1 | yes | spinner | blank | silent | once | On-demand fetch (button), real Apollo results gated on tenant company; 'No suggestions available.' empty state. Apollo unavailable returns empty with message (not surfaced in UI). External (Apollo), not stored, but real. Faithful enough. |
| Right-panel editable fields (name/domain/industry/size/revenue) | page.tsx:567-611 | account fields from GET /api/accounts/[id]; inline PUT via saveField (page.tsx:140-156, route.ts:188-241) | H1 | yes | skeleton | blank | silent | once | Real values with 'Set <field>' placeholder when null; optimistic PUT. saveField errors are swallowed silently on the keypress path (page.tsx:585-588) — minor. |
| Score (grade/heat/reasons) | page.tsx:612-646 | account.score / scoreReasons from GET /api/accounts/[id] (route.ts:175-176) | H1 | yes | skeleton | blank | global | once | Real score; shows 'Not scored' until enriched (industry+description) to avoid a misleading floor grade (page.tsx:617-642). Faithful. |

## Pires défauts

1. Contacts list is account-agnostic: page.tsx:90 calls /api/contacts?companyId=accountId, but contacts/route.ts (GET, lines 29-52) never reads searchParams.get('companyId') — so 'Contacts (N)' at api/contacts/route.ts returns the first 50 of ALL tenant contacts, not this account's. The properly-scoped set is already returned by api/accounts/[id]/route.ts:54-69 and thrown away (page.tsx:91-92).
2. Research Dossier masks server errors: a 500 from GET /api/research/dossier (research/dossier/route.ts:27-31) is caught as a generic non-ok and rendered as the 'No research dossier / Generate' CTA (company-dossier.tsx:129-131), so a backend failure looks like an empty state.
3. Silent error degradation on several mutate/fetch paths: contacts fetch only console.warn (page.tsx:93), inline field-save errors swallowed on Enter path (page.tsx:585-588), owner reassign errors swallowed (page.tsx:128-130) — no user-visible error state on these lanes.

## Résolution (P1 05 residue)

- **Defect #1 (contacts account-agnostic):** FIXED earlier as R2/T3 (`77762b0f`) — the contacts route now honors `?companyId`. Done.
- **Defect #2 (dossier masks errors):** FIXED. `company-dossier.tsx` now has a distinct `loadError` flag set on `!res.ok`/catch in `fetchDossier` (was collapsed into the same `setDossier(null)` as a genuine no-dossier 200). A new `role="alert"` branch renders BEFORE the "Generate dossier" CTA when `loadError && !dossier`: "Couldn't load the research dossier … the request failed." + a Retry button (`onClick={fetchDossier}`). A 500 no longer reads as "no dossier yet".
- **Defect #3 (silent field-save / owner-reassign swallows):** NOT changed in this pass — documented follow-up. These are write-path errors (a failed save leaves the field visibly unchanged, unlike a read-path mask that actively misleads), and surfacing them is a separate toast-migration task. Lower severity, deferred to stay in sensible scope.

Verdict after fix: the dossier lane (the H2 read-path mask) is now H1. Field-save/owner write-path swallows remain a documented P2 follow-up. tsc clean.
