_credentials/
node_modules/
.env*
!.env.example
.vercel-prod.env
.next/
dist/
harness/
credentials/
.playwright-mcp/
# Local dev TLS CA bundle (NODE_EXTRA_CA_CERTS) — machine-specific, never commit
*.dev-ca-bundle.pem
.dev-ca-bundle.pem
.turbo/
tsconfig.tsbuildinfo
# Playwright outputs
playwright-report/
test-results/
# Ad-hoc verification screenshots saved to repo root (keep them out of git)
/police-search-fixed*.png
# Auto-generated Next.js ambient types
app/apps/admin/next-env.d.ts
_inngest-*.log
.claude/scheduled_tasks.lock
_research/raw/lightfield-extraction/edge-profile/

# Regenerated dev/build/redeploy server logs
_*.log

# Machine-specific / binaries / secrets — never commit
.cacerts.pem
.vercel/
cloudflared.exe
*.exe

# ── Local dev / verification scratch — never commit ──────────────────
# Throwaway verify + tally scripts (the `_`-prefix convention marks
# disposable dev artifacts), plus the session-minting helper.
_verify-*
app/apps/web/scripts/_*
app/apps/web/scripts/mint-session.mjs
_inngest-sink.mjs
# Internal audit notes + scratch backups.
_audit/
# Secrets minted for local verification (JWTs) — must never reach git.
_jwt*.txt
# Dev screenshot output + numbered root captures (NNN-name.png).
app/apps/web/screenshots/
/[0-9][0-9][0-9]-*.png
