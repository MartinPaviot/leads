import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Call Mode",
};

export default function CallModeLayout({ children }: { children: React.ReactNode }) {
  return children;
}
