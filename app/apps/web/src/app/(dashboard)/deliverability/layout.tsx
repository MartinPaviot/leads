import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Deliverability",
};

export default function DeliverabilityLayout({ children }: { children: React.ReactNode }) {
  return children;
}
