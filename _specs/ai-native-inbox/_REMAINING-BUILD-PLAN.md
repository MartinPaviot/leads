# AI-native inbox — remaining-work build plan (triage wf w602vr4cq, 26 units)

Read-only triage by 26 Explore agents over the live worktree code + specs. Each verdict
is a build-or-flag decision against the bar (tsc + vitest + safe-by-construction, NO prod
schema migration, READ-ONLY mail, injectable-generator LLM, no live Inngest). LOC + completeness
are agent estimates. "buildable" = ship next; "flag-decision"/"ocean" = needs Martin or an ocean.

## Disposition

| Verdict | Units |
|---|---|
| buildable | R12, O02, X01, G04, K02+K04+K05, O03+O05, G10, G13, CAL03, R09, C01+G08, C10, Q01+Q02+Q05, N01+N02+N03, CAL02+CAL04, C12, P03, G02, G12, P04, P05, C05, T11+O06, P06 |
| flag-decision | X03+X04+X05+X06, Q06+Q08 |

## buildable (24)

### R12 — R12 — Calendar invite (.ics) inline render + RSVP
- **verdict** buildable · **storage** activities.metadata JSONB · **completeness** 85/10 · **~1200 LOC**
- **approach** Extract text/calendar MIME parts during inbound capture (pure parse) and store raw ICS in activities.metadata.icsData without schema migration. Add ics-parse.ts as a pure RFC 5545 parser sibling to the existing ics.ts builder. Wire the RSVP endpoint to the existing multi-provider calendar-write.ts and optional SMTP reply. Render an inline card reusing MeetingSchedulerCard styling, with Accept/Tentative/Decline buttons scoped to the viewer's calendar. Conflict detection reads the user's existing calendar via the read path already used by booking.
- **files** lib/integrations/ics-parse.ts, lib/integrations/ics-parse.test.ts, app/api/inbox/invite/rsvp/route.ts, app/(dashboard)/inbox/_rsvp-card.tsx, app/(dashboard)/inbox/_conversation-pane.tsx
- **plan**
  - 1. Implement lib/integrations/ics-parse.ts: pure RFC 5545 parser extracting VEVENT, METHOD, SEQUENCE, TZ, DTSTART/DTEND, SUMMARY, LOCATION, ORGANIZER, ATTENDEES, RRULE (summary only). Test REQUEST/CANCEL/update/recurring/all-day/floating-time fixtures (8+ tests).
  - 2. Add icsData?: {raw: string; parsed?: ParsedEvent} to ConversationMessage type in app/(dashboard)/inbox/_types.ts. Update conversations.ts to extract text/calendar from metadata when loading.
  - 3. Capture-path wiring: modify lib/capture/email-capture.ts or sync-functions.ts to call ics-parse on inbound messages and store {raw, parsed} in activities.metadata.icsData when METHOD:REQUEST detected. Add detection test.
  - 4. Implement POST /api/inbox/invite/rsvp route: accept(action:accept|tentative|decline, conversationKey), resolve user's calendar via existing getCalendarClient/getMicrosoftAccessToken/findSmtpMailbox, update event status via calendar-write module. Return {success, eventId, error?}. Add route tests with mocked providers.
  - 5. Create _rsvp-card.tsx component: render summary/time/location/organizer/attendees/recurrence, RSVP buttons (+conflict notice), no-calendar fallback (Download .ics / Add to calendar), card styled per MeetingSchedulerCard idiom (--color-bg-card, --color-border-default, --color-success/warning/error). No emoji, cited.
  - 6. Wire _rsvp-card into _conversation-pane.tsx: detect icsData in message, render card above EmailBody when present (matching MeetingSchedulerCard placement pattern). Reuse onTriage for done+snooze flow.
  - 7. Add unit tests: ics-parse.test.ts (8 fixtures), capture integration test, route test (mocked calendar), card render test. All tsc-clean, vitest-green.
- **residual** Conflict-aware UI pre-RSVP: reading the user's calendar at render-time vs. async fetch (deferred for runtime verify). METHOD:REPLY email composition and SMTP send on RSVP (depends on existing smtp-send.ts but routing to organizer requires metadata.organizerEmail parsed reliably). Multi-VEVENT handling (spec says "render each or primary"; implementation: parse first primary, log others). Timezone rendering accuracy (note assumed TZ if float time) deferred for browser runtime. Calendar API errors (transient failures, retry UX) deferred.
- **evidence** lib/integrations/ics.ts:1-132 (builder exists, parser missing); lib/integrations/calendar-write.ts:1-30 (multi-provider write path available); app/api/inbox/conversations/detail/route.ts:1-30 (metadata loaded); app/(dashboard)/inbox/_types.ts:65-81 (ConversationMessage interface, no icsData field); db/schema/core.ts:235-264 (activities.metadata JSONB at line 248); app/(dashboard)/inbox/_conversation-pane.tsx:33,442,670 (MeetingSchedulerCard and EmailBody placement); _PHASE2-WORKLIST.md:137 (R12 marked [ ] to build); lib/inbox/load.ts:27,82 (metadata loaded from activities).

### O02 — INBOX-O02 AI memory / standing instructions
- **verdict** buildable · **storage** user_preferences JSONB · **completeness** 9/10 · **~450 LOC**
- **approach** O02 is fully buildable within the bar using the injectable-generator pattern (proven by S01/S08/C04/Q07). The spec requires: (1) `lib/inbox/ai-memory.ts` to load personal standing instructions + aboutMe facts from user_preferences:inbox and company Knowledge (stages.ts + knowledge_entries), with buildMemoryPrompt() that composes them—personal precedence on conflict. (2) PUT endpoint via existing /api/user-preferences (upsert pattern already proven at route.ts:56-93). (3) Inject buildMemoryPrompt alongside buildWritingStylePrompt into compose/summarize/rewrite/ask endpoints—all at app/api/inbox/. (4) "Why" affordance mapping instruction/entry IDs to ConfidenceState styling (already at components/ai-ui/confidence-state.tsx). (5) Caps + conflict hint + auto-send guard in ai-memory.ts. No schema migration; READ-ONLY on mail; injectable LLM for deterministic tests.
- **files** lib/inbox/ai-memory.ts, lib/inbox/__tests__/ai-memory.test.ts, app/api/user-preferences/route.ts, app/(dashboard)/settings/inbox-memory/page.tsx, app/api/inbox/compose/draft/route.ts, app/api/inbox/compose/rewrite/route.ts, app/api/inbox/conversations/summarize/route.ts, app/api/inbox/conversations/ask/route.ts, lib/writing-profile.ts, lib/inbox/summarize-thread.ts, lib/inbox/rewrite.ts, lib/inbox/ask-thread.ts, components/ai-ui/confidence-state.tsx
- **plan**
  - Step 1: Create lib/inbox/ai-memory.ts — loadInboxMemory(authCtx) returns {standingInstructions[], aboutMe, knowledge}, buildMemoryPrompt() formats for injection, with personal-over-company precedence logic. Scope tenant + user via authCtx. Test: precedence, empty case, scope isolation.
  - Step 2: Create app/(dashboard)/settings/inbox-memory/page.tsx — 'Memory' UI card with standing-instructions list (add/edit/remove via Tag+Input idiom, reuse mail-calendar.tsx pattern ~743-754) + aboutMe fields (LabeledField inputs: signOffName, companyLine, keyColleagues[], defaultCc[]). Save via PUT /api/user-preferences resource:inbox keys:standingInstructions,aboutMe.
  - Step 3: Inject buildMemoryPrompt into inbox AI endpoints: app/api/inbox/compose/draft/route.ts, app/api/inbox/compose/rewrite/route.ts, app/api/inbox/conversations/summarize/route.ts, app/api/inbox/conversations/ask/route.ts. Prepend buildMemoryPrompt output (alongside buildWritingStylePrompt) in each prompt. Test: verify instruction appears in generated prompt.
  - Step 4: Wire 'why' affordance — map instruction/Knowledge entry ID that shaped output to ConfidenceState or a new 'instruction-applied' chip showing "Signed 'Martin' — your standing instruction" (reuse confidence-state.tsx styling). Surface on draft/summary pane.
  - Step 5: Implement caps + guards in ai-memory.ts: enforce item count (suggest 5-10 items), per-item length (suggest 500 chars), truncate *input* at save with counter, warn when cap reached. Add resolveAutoSend guard that detects 'auto-send' or 'never ask' instructions and tags them as 'not applied' (return { applied: false, reason }).
  - Step 6: Write vitest tests in lib/inbox/__tests__/ai-memory.test.ts covering: loadInboxMemory returns composed personal+company memory, personal precedence over Knowledge, multi-user scope isolation, empty case, cap enforcement, auto-send guard rejection.
- **residual** Browser/Playwright runtime verify of draft showing "Signed 'Martin' — your standing instruction" is deferred (Playwright harness down per PHASE2-WORKLIST line 11). Voice calibration + density settings (O03, O05) are separate specs. Shared inbox / per-message assignment (X01) is separate and references the same precedence/cap patterns but adds activities.metadata assignee. Deep Q07 (dock-pinned inbox_thread chat + CRM grounding + persisted history) is a follow-up, not in this buildable slice. Cascade delete on auth_user deletion is pre-existing DB behavior (auth.ts line 147 onDelete cascade). Integration with zero-retention flag (P03) is scoped to header-gating in llm-call path—must verify at call sites whether memory is included under retention opts."
- **evidence** db/schema/auth.ts:141-164 — user_preferences table with (userId, resource, key) unique index + JSONB value; no migration needed. app/api/user-preferences/route.ts:56-93 — upsert pattern already proven. lib/inbox/summarize-thread.ts, lib/inbox/rewrite.ts, lib/inbox/ask-thread.ts — injectable-generator pattern (buildXxxPrompt + defaultGenerate with lazy AI SDK import + fail-closed) proven to work. lib/writing-profile.ts:37-49 — buildWritingStylePrompt already prepends to prompts in app/api/emails/route.ts:100+ (shown at line 7, 76). lib/knowledge/stages.ts:27-98 — KNOWLEDGE_STAGES + effectiveStages helpers ready to reuse for 'outreach'+' global' injection. app/(dashboard)/settings/mail-calendar/page.tsx:1-300+ — Tag+Input add-row idiom at lines ~743-754 (mentioned in spec; use as UI template). components/ai-ui/confidence-state.tsx — ConfidenceState component ready for 'why' chips. lib/inbox/autonomy.ts:32-34 — hard guard on 'send' action proves never-auto-send contract. _PHASE2-WORKLIST.md line 129 — O02 marked [ ] to build, with explicit strategy 'user_preferences JSONB + inject'.

### X01 — Shared inbox + per-message assignment (X01)
- **verdict** buildable · **storage** activities.metadata JSONB (no schema migration) OR lightweight inbox_assignment table with same shape as inbox_triage · **completeness** 9/10 · **~450 LOC**
- **approach** Add shared mailbox flag to connected_mailboxes; extend getInboxScope to union shared mailboxes (member/admin read-only for viewers); create POST /api/inbox/assign route (tenant-scoped, audit-logged, viewer-blocked centrally); wire assignee chip + OwnerSelect into list row and pane header; add \"Assigned to me\" tab filtering by assignee_user_id = viewer; implement deactivated-assignee fallback (resolve against active members only). Storage strategy: store assignee_user_id keyed on (tenant_id, conversation_key) in activities.metadata with collision-safe merge, or use a dedicated inbox_assignment table mirroring inbox_triage's shape and unique index.
- **files** lib/inbox/user-scope.ts, app/api/inbox/conversations/route.ts, app/api/inbox/assign/route.ts, app/(dashboard)/inbox/page.tsx, app/(dashboard)/inbox/_conversation-list.tsx, app/(dashboard)/inbox/_conversation-pane.tsx, db/schema/outbound.ts, db/schema/core.ts, lib/infra/audit-log.ts, lib/auth/viewer-guard.ts
- **plan**
  - 1. Extend connected_mailboxes schema: add `shared boolean default false` column and index it (db/schema/outbound.ts:218+). Run drizzle generate and verify schema-shape test.
  - 2. Expand getInboxScope (lib/inbox/user-scope.ts:44-67): after personal mailboxes, union shared mailboxes for members/admins where (shared = true AND tenant = mine). Viewers remain read-only on the scoped set.
  - 3. Create POST /api/inbox/assign route: upsert (tenant_id, conversation_key, assignee_user_id) keyed on unique (tenant_id, conversation_key); tenant-scoped auth; audit-log via logAudit() with actor=authCtx.userId, resource=conversation_key, action=assign; viewer write blocked by middleware (viewer-guard.ts).
  - 4. Extend GET /api/inbox/conversations route (app/api/inbox/conversations/route.ts): join inbox_assignment (or metadata) on conversation_key; accept ?lane=mine to filter assignee = authCtx.userId.
  - 5. Add assignee chip to ConversationList row (_conversation-list.tsx): render member name chip or "Unassigned" (using color-text-secondary token); omit control for viewers.
  - 6. Add assignee OwnerSelect to ConversationPane header (_conversation-pane.tsx): next to reason badge (~:277); disable for viewers; onClick → POST /api/inbox/assign.
  - 7. Add "Assigned to me" tab to inbox/page.tsx TABS constant (:51); wire ?lane=mine param to route.
  - 8. Implement deactivated-assignee fallback: in the route, resolve assignee_user_id against active members (isNull(deactivatedAt)); if deactivated, render as "Unassigned" but preserve audit trail.
  - 9. Add keyboard shortcut `a` to open assignee picker on selected row (inbox/page.tsx:182 keyboard handler).
  - 10. Unit tests: user-scope.test.ts for shared visibility (personal stays private, shared widens for member, viewer read-only); route test for assign idempotency + viewer rejection; dom test for assignee chip + viewer-hidden control.
- **residual** Runtime browser verify: assign persists across reload, viewer POST → 403, deactivated assignee reverts to \"Unassigned\" on next load, collision notice + assignment reinforce each other. Offline/members fetch fail: assignee picker degrades to \"Unassigned\" (fail-soft, like OwnerSelect catch path). Stale/cross-tenant assignee_user_id in assignment row: rejected by route auth; picker only offers /api/settings/members results (tenant-scoped). Multi-tenant isolation: DB-level withTenantTx or query-time filtering enforces that assignee resolve + shared-mailbox visibility never escape authCtx.tenantId (app-layer check verified; DB-level RLS flagged in worklist P05 as residual)."
- **evidence** Spec file INBOX-X01.md:1-141 defines full feature (shared flag, per-message assignment, lanes, deactivated fallback, security gates). Worklist _PHASE2-WORKLIST.md:119-120 marks X01 as [M] MISSING: \"no inbox_assignment table/column, no /assign route, no assignee in API/UI. Per-message assignee storable in activities.metadata JSONB (no migration).\" Verified via grep: connected_mailboxes (db/schema/outbound.ts:218) exists but no `shared` column. No inbox_assignment table found (grep -rn inbox_assignment /db → empty). No /api/inbox/assign route (find /api/inbox yields 14 routes, no assign). getInboxScope (lib/inbox/user-scope.ts:56) currently filters only eq(connected_mailboxes.userId, authUserId), no shared union. activities.metadata JSONB confirmed (db/schema/core.ts:248). OwnerSelect component exists (components/owner-select.tsx:22-86), fetches /api/settings/members, returns tenant-scoped active members. viewer-guard.ts (lib/auth/viewer-guard.ts:37-46) blocks viewer POSTs outside allowlist. logAudit pattern used for role changes (api/settings/members/route.ts:100+). inbox_triage table (db/schema/outbound.ts:370-385) establishes unique(tenant_id, conversation_key) pattern for assignment-like rows. Inbox page TABS constant (inbox/page.tsx:51) lists [\"attention\", \"snoozed\", \"done\", \"handled\", \"outbound\"], no \"assigned to me\" yet.

### G04 — Signal surfacing (funding / hiring / intent) in-thread (freshness-gated, cited)
- **verdict** buildable · **storage** pure-compute + user_preferences JSONB · **completeness** 9/10 · **~230 LOC**
- **approach** Reuse the existing freshness module (lib/signals/freshness.ts:31,80,100) that already gates all signal display in the app. Create a new lib/inbox/signal-surface.ts that assembles and filters fresh company signals from companies.properties, following the pattern of lib/context/prospect-context.ts:138-149. Wire this into the GET /api/inbox/conversations/detail route alongside nextAction/lastInteraction. Surface signals in _conversation-pane.tsx using a new Signaux sidebar chip row (family-grouped, cited tooltips, no provider names). The optional handoff to G08 stays residual (runtime verify).
- **files** lib/signals/freshness.ts, lib/inbox/signal-surface.ts, app/api/inbox/conversations/detail/route.ts, app/(dashboard)/inbox/_conversation-pane.tsx, app/(dashboard)/inbox/_types.ts
- **plan**
  - 1. Create lib/inbox/signal-surface.ts: export assembleCompanySignals(companyProps, now?) that detects active signals from companies.properties, filters via filterFreshSignals(), shapes each as { family, label, observedAt, citation:"via Elevay" }, groups by family, orders by recency. Pure, testable, 60 LOC.
  - 2. Extend GET /api/inbox/conversations/detail to fetch company signals: load conversation's company, call assembleCompanySignals(), return signals[] in response JSON. Add 50 LOC to route.ts.
  - 3. Extend _types.ts ConversationDetail interface: add signals: Array<{family, label, observedAt}> field. Pure type change.
  - 4. Add 'Signaux' sidebar sub-section in _conversation-pane.tsx: render signal chips grouped by family (TrendingUp for funding, Users for hiring, MousePointer for intent, UserCog for leadership, Cpu for tech). Each chip has lucide icon, sober background (--color-accent-soft), tooltip citing date ("via Elevay · il y a 2 mois"), no provider name. Empty state renders nothing. Add ~120 LOC to pane component.
  - 5. Unit tests: test signal-surface with TTL boundaries (60-day-old funding kept, 45-day-old hiring dropped, 10-day-old website_visit dropped, investor_overlap always fresh, signal with no observedAt kept). Test route: mock company signal fixture, verify API returns fresh+cited signals. Test pane render + empty state. ~8 tests across 2 files.
- **blockers** None. All dependencies exist: freshness module is wired, company.properties schema holds signals, contact resolution in detail route works, pane renders other derived fields like nextAction.
- **residual** Optional handoff to G08 (grounded draft): clicking a signal chip to seed a reply draft. This requires wiring into the compose context + testing reply generation with fresh signal context — stay residual for runtime verify. Future: CJK/multilingual relative-age labels beyond "il y a 2 mois" (i18n per tenant UI language).
- **evidence** lib/signals/freshness.ts:31 (SIGNAL_TTL_DAYS), :80 (isSignalFresh), :100 (filterFreshSignals) are the SSOT. lib/context/prospect-context.ts:138-149 show signal filtering pattern already in use. db/schema/core.ts:63 (companies.properties as JSONB). _conversation-pane.tsx:5 (comment states no signal surface exists). _types.ts:73 (ConversationDetail interface, no signals field yet).

### K02 + K04 + K05 — AI-native inbox keyboard shortcuts, prefetch, and quick-switch—buildable cluster
- **verdict** buildable · **storage** pure-compute (K02 registry + K04 LRU cache + K05 state routing) · **completeness** 9/10 · **~850 LOC**
- **approach** K02 mounts the already-built ShortcutHelp overlay in layout.tsx and migrates raw keyboard handlers in use-keyboard-shortcuts.ts + inbox page.tsx to useHotkey() calls so they auto-register in the live registry. K04 adds a tiny client-side LRU cache keyed by conversationKey and triggers prefetch on hover/focus of rows + next-row auto-prefetch on selection change (no new endpoint). K05 wires m-chord handler (mirroring g-chord pattern) to switch selectedMailbox, refetch lane scoped, and adds a "Switch mailbox" command group to the palette—hiding all UI when single-box.
- **files** lib/inbox/keybindings.ts, lib/hotkey-registry.ts, hooks/use-hotkey.ts, hooks/use-keyboard-shortcuts.ts, components/ui/shortcut-help.tsx, app/(dashboard)/layout.tsx, app/(dashboard)/inbox/page.tsx, app/(dashboard)/inbox/_conversation-list.tsx, app/(dashboard)/inbox/_conversation-pane.tsx, app/(dashboard)/inbox/_mailbox-rail.tsx, app/(dashboard)/inbox/_command-palette.tsx
- **plan**
  - K02.1: Mount &lt;ShortcutHelp /&gt; in app/(dashboard)/layout.tsx next to &lt;CommandPalette /&gt; (file:84-120)
  - K02.2: Migrate Cmd+K//n/?/g-chords in use-keyboard-shortcuts.ts:48-113 to useHotkey() calls with description+group, OR register them explicitly in hotkey-registry
  - K02.3: Register inbox j/k/e/r/x/Escape shortcuts in page.tsx:352-408 via useHotkey() when inbox mounts, group='Inbox'
  - K02.4: Create /settings/shortcuts static reference page enumerating full catalog
  - K02.5: Wire prettyCombo to honor K07 custom bindings from user_preferences (deferred—test with hardcoded defaults)
  - K04.1: Add inbox-prefetch.ts with detailedCache (LRU Map, ~100KB cap) + prefetchDetail(key) helper, abort-safe
  - K04.2: Add onMouseEnter/onFocus prefetch to rows in _conversation-list.tsx:86-96 via a usePrefetch(key, {onHover:true}) hook, guarded by pointer-fineness
  - K04.3: Next-row prefetch on selection change in page.tsx useEffect (computed from conversations+selectedKey)
  - K04.4: Pane reads cache first in _conversation-pane.tsx:128 before fetching, revalidates if stale (background)
  - K04.5: Verify sidebar Link prefetch is on (Next.js default) and progress bar fires on nav click
  - K05.1: Add m-chord handler to page.tsx:352 keyboard effect, map 1-9→mailboxes[n], 0/a→All, register in shortcut registry, typing-safe, only 2+ boxes
  - K05.2: On chord match: update selectedMailbox, refetch lane scoped (?mailbox=), reset selection to first row, preserve lane tab
  - K05.3: Add 'Switch mailbox' command group to _command-palette.tsx buildItems(), each item shows address+backlog count
  - K05.4: Hide rail + chords + palette group when mailboxes.length &lt; 2
- **residual** K07 custom keybindings: reading custom combos from user_preferences is deferred; tests use defaults. K04 stale-revalidation background refresh timing (30 s TTL suggested). K05 mailbox disconnect edge case (clamp index gracefully). K02 static /settings/shortcuts page content audit (ensure every global+inbox+K06 additions listed). K04 touch/coarse-pointer detection (media query). All three: runtime E2E verify with Playwright (browser navigation, hover prefetch visual latency, chord responsiveness).
- **evidence** K02 mounting gap: file:C:/Users/ombel/leads-wt-inbox/app/apps/web/src/app/(dashboard)/layout.tsx:84-119 shows CommandPalette/ChatDock/IdleLogout mounted but NO ShortcutHelp. K02 component exists: file:C:/Users/ombel/leads-wt-inbox/app/apps/web/src/components/ui/shortcut-help.tsx:17-119. Raw keyboard handlers: file:C:/Users/ombel/leads-wt-inbox/app/apps/web/src/hooks/use-keyboard-shortcuts.ts:48-113 (no useHotkey), file:C:/Users/ombel/leads-wt-inbox/app/apps/web/src/app/(dashboard)/inbox/page.tsx:352-408 (no registry). K04 zero-prefetch: Grep found no 'prefetch' in codebase. Detail fetch happens only after selectedKey changes: file:C:/Users/ombel/leads-wt-inbox/app/apps/web/src/app/(dashboard)/inbox/_conversation-pane.tsx:128. List rows structure: file:C:/Users/ombel/leads-wt-inbox/app/apps/web/src/app/(dashboard)/inbox/_conversation-list.tsx:86-96. K05 mailbox model exists: file:C:/Users/ombel/leads-wt-inbox/app/apps/web/src/app/(dashboard)/inbox/page.tsx:67-68 (selectedMailbox + mailboxes state), file:C:/Users/ombel/leads-wt-inbox/app/apps/web/src/app/(dashboard)/inbox/page.tsx:116 (?mailbox= already in query). Rail mouse-only: file:C:/Users/ombel/leads-wt-inbox/app/apps/web/src/app/(dashboard)/inbox/_mailbox-rail.tsx:77-96 only has onClick, no chord. Global g-chord pattern to mirror: file:C:/Users/ombel/leads-wt-inbox/app/apps/web/src/hooks/use-keyboard-shortcuts.ts:60-85.

### O03 + O05 — AI-native inbox voice calibration + display density (settings UI + LLM integration)
- **verdict** buildable · **storage** user_preferences JSONB (resource "inbox", keys "voice" and "density") · **completeness** 9/10 · **~380 LOC**
- **approach** O03 and O05 are both BUILDABLE within the bar using injectable-LLM + JSONB-only storage, with no schema migration. They reuse three proven patterns already deployed: (1) `user_preferences` k-v JSONB store (seen-store.ts/lane-store.ts/filter-store.ts model), (2) injectable-generator for LLM features (S02/S06/C04/Q07 precedent), (3) existing ThemeProvider + DisplayPanel components. O03 extends the draft path to read voice overrides from user_preferences and apply them over the already-integrated buildWritingStylePrompt + getWritingSamples (live since emails-api.ts + chat/tools/action.ts). O05 extends ThemeProvider to hydrate theme from user_preferences (keeping localStorage as the no-flash fast path) and wires inbox density into the list/pane row constants. Both are safe-by-construction: read-only on user preferences, pure JSONB append, no migrations.
- **files** lib/inbox/voice-settings.ts, app/api/inbox/voice-preview/route.ts, app/(dashboard)/settings/mail-calendar/page.tsx, components/ui/theme-provider.tsx, lib/writing-profile.ts, components/ui/display-panel.tsx
- **plan**
  - Create lib/inbox/voice-settings.ts: getVoiceSettings(userId) and setVoiceSettings(userId, {greeting?, signOff?, formality?, length?}) using user_preferences JSONB pattern (resource='inbox', key='voice', stored as json value object)
  - Create POST /api/inbox/voice-preview endpoint to return a sample reply using buildRewritePrompt + voice overrides for UI preview
  - Extend draft paths (emails/route.ts + inbox compose if added) to read voice overrides from user_preferences and inject into buildWritingStylePrompt output before LLM call
  - Extend ThemeProvider to: (a) load theme from user_preferences on mount (resource='inbox', key='theme'), (b) apply .dark class, (c) persist changes via PUT /api/user-preferences, (d) keep localStorage in sync for no-FOUC
  - Wire inbox density (user_preferences resource='inbox', key='density' as DisplayDensity enum) into the list/pane row-height constants (--table-row scaled by compact/default/comfortable)
  - Create Settings UI: add Voice section to /settings/mail-calendar with: greeting/sign-off LabeledField inputs (text), formality/length segmented controls (casual|neutral|formal, shorter|match|longer), live preview button → POST /api/inbox/voice-preview (reuse _thread-preview-like block)
  - Add Display section to /settings/mail-calendar with: theme toggle (Light/Dark, reuse Button variant), density selector (reuse DisplayPanel segmented control), both with optimistic apply + background save + revert on failure
  - Token audit: verify inbox list/pane/badges/sidebars use --color-* tokens (no hardcoded hex) for dark-mode safety. Fix any raw hex.
  - Wire DisplayDensity into virtualization row-height constant (R11 follow-up; currently just list row padding)
- **residual** Runtime verification deferred (Playwright down; manual smoke-test on the Vercel preview PR #277 to confirm theme/density apply visually + voice preview returns sensible text + round-trip persistence works). Voice preview LLM call is stub-testable (inject mock generate), but the real "does the AI mimic this voice" bar requires reading real sent mail + live model. Per-user tenant-scope already guaranteed by getAuthContext() at route level (every endpoint enforces it). Density ↔ virtualization (R11) row-height sync is a placeholder until R11 is built. Email-body dark rendering (INBOX-R08) is out-of-scope; O05 themes the app chrome only. Saved searches (INBOX-Q04 residual) are separate.
- **evidence** INBOX-O03.md:84-86 design sketch reuses lib/writing-profile.ts (exists, file:1, getWritingSamples + buildWritingStylePrompt proven in emails-api.ts:15-29 + chat/tools/action.ts:8-12). INBOX-O03.md:26-31 voice overrides on user_preferences (no migration, reuses the JSONB k-v model). INBOX-O05.md:71-77 theme+density stored in user_preferences (resource inbox, no migration). theme-provider.tsx:16-40 currently persists to localStorage only, no user_preferences call. display-panel.tsx:11,17,63-65 defines DisplayDensity enum + segmented control, already persisted per spec comment:24 "via /api/user-preferences separately". user-preferences/route.ts:20-99 is the reusable endpoint (GET/PUT resource k-v). seen-store.ts:14-38 demonstrates the exact pattern for both O03 and O05 (user_preferences JSONB CRUD). INBOX-O03.md:105-115 task list (5 items) + INBOX-O05.md:89-101 task list (5 items). PHASE2-WORKLIST.md:121,136 marks O03/O05 as UI-RESIDUAL but now buildable via settings pages (no "interactive" blocker remains if we treat LLM preview as stub-testable). The injectable-generator pattern from S02/S06/C04/Q07 (see _EXECUTION-LOG.md:138-161) allows voice-preview to be deterministic in tests (mock generate, pass fixed prompt) and live in production (lazy-load AI SDK).

### G10 — G10 — Meeting-booked → CRM + sovereign visio (residual: inject join link into reply draft)
- **verdict** buildable · **storage** pure-compute · **completeness** 9/10 · **~80 LOC**
- **approach** The core meeting booking + sovereign visio + CRM activity logging is fully shipped (video-meeting.ts:141 mints Jitsi rooms, calendar-write.ts:96 books via bookSovereignMeeting, /api/meetings/book:43 enforces deep-dive capacity, logs meeting_scheduled activities at :150). The residual LAKE work is thin: pass onBooked callback from _conversation-pane.tsx to MeetingSchedulerCard, capture the joinUrl response, and inject it into the composer.body before showing the reply. No schema migration, no LLM, pure state management.
- **files** lib/integrations/video-meeting.ts, lib/integrations/calendar-write.ts, app/api/meetings/book/route.ts, components/meeting-scheduler.tsx, app/(dashboard)/inbox/_conversation-pane.tsx
- **plan**
  - 1. Update MeetingSchedulerCard call in _conversation-pane.tsx:445 to pass onBooked callback that opens or pre-fills the reply composer with the join link injected into the draft body.
  - 2. Capture joinUrl from the booking response in meeting-scheduler.tsx:100 (already available), invoke onBooked callback with the URL.
  - 3. In the pane's onBooked handler, update composer state with setComposer(...old, body: ...insertJoinUrlIntoBody(oldBody, joinUrl)...) or open reply composer with pre-filled visio link.
  - 4. Verify join link is persisted in outboundEmails.bodyHtml/bodyText when draft is sent (no new DB changes needed).
  - 5. Test: book meeting, verify reply draft opens with visio link embedded, send, confirm email body contains the join URL.
- **residual** Runtime verify browser flow: click Book meeting → confirm time → reply composer auto-opens with join link in body → send → prospect receives invite with visio URL. Autonomy dial gating for deal-advance (INBOX-G05/G09) is flagged in spec but not required for the core G10 booking+visio. Multi-language localization of "Rejoindre la visio" (currently hardcoded French per calendar-write.ts:83, 86, 218) deferred to UI polish pass.
- **evidence** C:/Users/ombel/leads-wt-inbox/app/apps/web/src/lib/integrations/video-meeting.ts:1-155 (createSovereignMeeting is pure, no vendor API), C:/Users/ombel/leads-wt-inbox/app/apps/web/src/lib/integrations/calendar-write.ts:96-162 (bookSovereignMeeting resolves provider, calls injected link, returns joinUrl:243), C:/Users/ombel/leads-wt-inbox/app/apps/web/src/app/api/meetings/book/route.ts:43-190 (route enforces capacity at :76, returns joinUrl:179, logs meeting_scheduled:150 with metadata), C:/Users/ombel/leads-wt-inbox/app/apps/web/src/components/meeting-scheduler.tsx:27,32,97 (onBooked callback defined, called after booking), C:/Users/ombel/leads-wt-inbox/app/apps/web/src/app/(dashboard)/inbox/_conversation-pane.tsx:445-446 (MeetingSchedulerCard called WITHOUT onBooked), C:/Users/ombel/leads-wt-inbox/app/apps/web/src/lib/accounts/last-interaction.ts:23 (meeting_scheduled in INTERACTION_ACTIVITY_TYPES for G03 timeline), C:/Users/ombel/leads/_specs/ai-native-inbox/INBOX-G10.md:35-36 (requirement: insert join URL into reply draft G08 junction), C:/Users/ombel/leads/_specs/ai-native-inbox/_PHASE2-WORKLIST.md:110 (residual: onBooked → inject join link into reply draft).

### G13 — G13 — MCP server + agent Skills (GTM-grounded inbox/CRM)
- **verdict** buildable · **storage** user_preferences JSONB (Skills enable/schedule state) + activities.metadata JSONB (via clobber-safe || merge for captured interactions in review mode) + injectable-generator LLM (lazy import() of AI SDK inside defaultGenerate, injectable generate param for tests, fail-closed) · **completeness** 9/10 · **~2200 LOC**
- **approach** Extend the existing native-CRM MCP server (app/api/mcp/route.ts) by adding 7 inbox/GTM tools (search_inbox, summarize_thread, draft_reply, list_signals, last_interaction, book_meeting, log_interaction) that delegate to pre-built seams: loadConversationRows + scopeConversationRows (per-user scope via keyOwnerId), the S01/S08 summarizer, INBOX-C01/G08 draft context, recordCapturedActivity (for review-mode capture gating), filterFreshSignals, and the G03 last-interaction union. Add per-user mailbox scope enforcement via keyOwnerId on mail-reading tools. Implement 4 revenue-native Skills (Deal Tracker, Morning Briefing, End-of-Day Wrap-Up, Batch Draft Writer) as Inngest cron compositions, each reusing the same MCP-tools-as-seams pattern and publishing citations. Gate mutating tools with capture-approval + autonomy checks. Extend the /settings/mcp admin page to show the new tool catalog, per-key owner/mail-scope indicator, and Skills enable/schedule controls.
- **files** app/api/mcp/route.ts, app/api/mcp/keys/route.ts, app/(dashboard)/settings/mcp/page.tsx, lib/inbox/user-scope.ts, lib/inbox/load.ts, lib/inbox/summarize-thread.ts, lib/inbox/ask-thread.ts, lib/inbox/rewrite.ts, lib/inbox/draft-from-bullets.ts, lib/capture/approval.ts, lib/signals/freshness.ts, lib/accounts/last-interaction.ts, app/api/meetings/book/route.ts, inngest/skill-crons.ts, skills/skill-knowledge.ts
- **plan**
  - 1. Add inbox/GTM tools to MCP_TOOLS array + tool definitions in handleTool (route.ts:19-226): search_inbox, summarize_thread, draft_reply, list_signals, last_interaction, book_meeting, log_interaction; each with documented parameters and return shape.
  - 2. Implement each tool handler in route.ts delegating to existing seams: search_inbox → loadConversationRows + scopeConversationRows; summarize_thread → summarize-thread.ts (injectable); draft_reply → draft context (grounded, never sends); list_signals → filterFreshSignals (freshness.ts); last_interaction → lastInteractionUnionSql; book_meeting → POST /api/meetings/book (sovereign); log_interaction → recordCapturedActivity.
  - 3. Enforce per-user mailbox scope on mail tools via keyOwnerId: pass keyOwnerId from authenticateMcpRequest result into search_inbox handler, resolve user's InboxScope via getInboxScope(tenantId, keyOwnerId), apply scopeConversationRows filter.
  - 4. Gate mutating tools: draft_reply returns a draft object, does NOT call any send endpoint; log_interaction honors capture-approval mode (recordCapturedActivity handles review/auto) and returns approval-id when in review mode; book_meeting calls the sovereign route and returns error if no mailbox connected.
  - 5. Create 4 revenue-native Skills in inngest/skill-crons.ts (or new skill/*.ts files): Deal Tracker (runSkill composition: search_inbox for thread history + last_interaction + list_signals + list_deals for the company); Morning Briefing (overnight email_received messages + at-risk deals + today's meetings); End-of-Day Wrap-Up (sent threads + deal updates); Batch Draft Writer (flagged threads → draft_reply for each, cites sources). Each skill composes MCP tools and publishes {title, body, sources[]} citations.
  - 6. Wire Skills into the cron scheduler (skill-crons.ts) and add per-tenant enable/schedule settings to user_preferences JSONB (Skills panel in /settings/mcp allows users to toggle Deal Tracker + Morning Briefing + End-of-Day + Batch Writer and set schedule or listen for manual trigger).
  - 7. Extend /settings/mcp UI (mcp-client.tsx) to display: tool catalog (read-only list of all 12+7 tools with descriptions), per-key owner + mail-scope indicator (show keyOwnerId if present), Skills enable/schedule panel (toggles + cron schedule picker for each skill), Claude/Cursor connection snippet (tenant-name + base URL + key).
  - 8. Add audit logging for every MCP tool call: tenant, keyId, tool name, timestamp, input summary, result summary (like authenticateMcpRequest already does). Rate-limit generative tools (search_inbox, summarize_thread, draft_reply) per key to prevent abuse.
  - 9. Add comprehensive unit tests: per-tool scope enforcement (tenant-B id → not found), per-user mailbox scope (workspace key reads only owner's mail), draft_reply never sends, log_interaction in review mode parks approval, Skills skill-quality-config patterns for citation validation.
- **blockers** None identified. All seams exist; injectable pattern is established and proven in S01/S02/C04/C07/Q07; per-user scope infrastructure (user-scope.ts + getInboxScope) is ready; capture-approval mode is wired; Skills cron framework exists with runSkill + skill-quality-config; settings UI admin-only + prod-gated already enforced.
- **residual** Runtime verification of end-to-end Skills execution (Integration tests with live Inngest cron trigger), Skills scheduling UI date-picker + cron expression builder (can fold into existing admin settings patterns), browser/e2e verification of /settings/mcp page rendering and Skills enable/schedule flow (Playwright defer per worklist), zero-retention header propagation into LLM-call seams for summarize_thread + draft_reply (verify summarize.ts + rewrite.ts honor retention policy; likely needs tenant aiProcessingProfile check passed as context), cross-tenant MCP key isolation at scale (current full-tenant scan at route.ts:243 acceptable at pilot; index-on-key_prefix optimization flagged as follow-up per spec line 74-75), rate-limit implementation details (Redis or in-memory counter; defer to infra team).
- **evidence** MCP_TOOLS array at route.ts:19-226 shows 12 existing CRM tools (search_records, get_contact, get_company, get_deal, list_contacts, list_companies, list_deals, create_contact, create_deal, log_note, list_activities, search_crm); handleTool switch at route.ts:293-326 shows structure for tool delegation; authenticateMcpRequest at route.ts:230-287 shows Bearer token auth + keyHash verification + keyOwnerId captured at line 277 but not yet used; loadConversationRows at lib/inbox/load.ts:16-88 fetches tenant-wide inbound/outbound/triage rows; scopeConversationRows at lib/inbox/user-scope.ts:113-128 filters by scope.addresses (per-user mailbox scope); summarize-thread.ts:1-77 (injectable pattern with defaultGenerate at line 43); rewrite.ts:43-77 (injectable pattern); ask-thread.ts:78-102 (injectable defaultGenerate); recordCapturedActivity at lib/capture/approval.ts:80-89 gates on capture mode (review vs auto); filterFreshSignals at lib/signals/freshness.ts:30-60 (TTL enforcement); lastInteractionUnionSql at lib/accounts/last-interaction.ts:36-63 (union query); POST /api/meetings/book/route.ts:43-80 (sovereign booking with CalendarNotConnectedError); inngest/skill-crons.ts:1-100 shows Inngest cron pattern (weeklySignalScan) and runSkill invocation at line 71; skills/skill-knowledge.ts:14-48 shows skill context retrieval pattern; /settings/mcp/page.tsx:1-11 exists with MCP_PAGE_ENABLED + adminOnlyOrRedirect guards; keys/route.ts:49-120 shows keyOwnerId is captured at line 86 when creating keys."

### CAL03 — AI meeting scheduler (end-to-end, sovereign) — agentic stepper + multi-provider slots
- **verdict** buildable · **storage** pure-compute · **completeness** 9/10 · **~850 LOC**
- **approach** Extend getAvailableSlots (meeting-booking.ts) to resolve CalDAV/Microsoft free-busy alongside Google via a provider-agnostic refactor that keeps slot generation pure. Build POST /api/inbox/schedule (new route) to chain: resolve viewer's userId (via getAuthContext) → resolve contact+deal+stage+lastInteraction from the conversation key → compute multi-provider slots → draft voice-matched reply with sovereign link via injectable-generator LLM → return auditable steps + slots + draft. Inject this into _conversation-pane action bar and proactive meeting-request chip. Scope all reads to viewer (getInboxScope), inject-quarantine inbound via prompt-safety, honor capacity gate, re-validate slots at send. No persistence beyond request.
- **files** lib/integrations/meeting-booking.ts, lib/integrations/calendar.ts, lib/integrations/caldav.ts, lib/integrations/calendar-microsoft.ts, lib/integrations/calendar-write.ts, lib/integrations/video-meeting.ts, lib/writing-profile.ts, lib/inbox/user-scope.ts, lib/calendar/capacity.ts, lib/accounts/last-interaction.ts, lib/inbox/conversations.ts, app/api/inbox/schedule/route.ts, app/api/meetings/book/route.ts, app/(dashboard)/inbox/_conversation-pane.tsx, inngest/reply-handler.ts
- **plan**
  - Refactor getAvailableSlots(userId, opts) to multi-provider: (1) extract pure slot-generation logic from business-hours + weekend filtering into a new pure function slotGenerator(dateRange, windowStart, windowEnd, duration); (2) resolve user's connected calendar (CalDAV→Microsoft→Google order per calendar-write.ts:15) by decrypting caldav_url+secrets from connectedMailboxes (like calendar-write.ts:findSmtpMailbox does) and fetching free-busy per provider: Google via calendar.freebusy.query (existing), CalDAV via fetchCalDavMeetings+mapIcsToMeetings (existing at caldav.ts), Microsoft via Graph POST /me/calendar/getSchedule (use getMicrosoftAccessToken existing at calendar-microsoft.ts:75); (3) union busy periods across all connected calendars; (4) subtract from slot candidates; (5) return AvailableSlot[] as before. Add unit tests: Google-user real slots, CalDAV-user real slots, Microsoft-user real slots, no busy slot returned, no provider errors sink the whole result.
  - Create POST /api/inbox/schedule route: (1) parse { conversationKey, durationMinutes?, count?, meetingType? }; (2) auth + getInboxScope scope-check; (3) load conversation detail via existing conversations.ts logic (reuse detail/route.ts pattern:32-35); (4) fetch contact+deal via contactId from conversation; (5) resolve GTM context: deal.stage + last-interaction via lib/accounts/last-interaction.ts + signals (reuse G01 prospect brief pattern); (6) call refactored getAvailableSlots(userId, durationMinutes, meetingType); (7) via injectable-generator LLM (claude-sonnet-4-6, fail-closed like S01/S08): choose meeting type+duration from stage, compose voice-matched reply grounded in context+slots+writing samples (getWritingSamples+buildWritingStylePrompt existing); (8) return { steps: [{ kind, summary, citations }], slots: [{ startIso, endIso, formatted }], draft: { subject, body }, conferencing: 'sovereign', calendarConnected }. Add route tests: cited steps, real slots, sovereign URL, no-calendar degrade, capacity gate honored.
  - Wire POST /api/inbox/schedule into UI: (1) add 'Schedule a call' action to _conversation-pane:291 action bar (use CalendarClock icon); (2) open a stepper card (use existing card bg/shadow/rounded tokens) showing step summaries + citations (reuse cited-text.tsx), selectable slot chips (each showing startIso + formatted), then draft preview with Send/Insert Draft buttons (Mirror compose flow C01/G08 approval gating); (3) proactive offer chip when conversation classification=meeting_request via existing classify-intent logic; (4) add shortcut g+s (go-schedule) to thread (like existing shortcuts).
  - Fix autonomous reply-handler scope bug: line 131-140 resolves 'first user in table' (db.select().from(users).limit(1)) instead of the context's enrollmentId→contact→tenantId. Change to: fetch user via enrollmentId→contact→userId (the contact's owner mailbox user, matching getInboxScope pattern); use that userId for getAvailableSlots. Verify multi-tenant isolation.
  - Hardening: (1) per-user/tenant scope (getAuthContext + getInboxScope pattern, verified in ask/route.ts); (2) inject inbound thread via lib/chat/prompt-safety.ts (existing, already used in ask-thread.ts); (3) capacity gate (decideDeepDiveBooking existing at capacity.ts); (4) slot re-validation at draft + send (calendar.freebusy.query idempotent re-check before returning 200); (5) zero-retention (computed slots + draft request-scoped, not persisted, clamped to memory); (6) rate-limit the endpoint (standard Vercel pattern or Inngest guard).
- **blockers** None. All building blocks exist. The refactor of getAvailableSlots to multi-provider is pure-function safe and backward-compatible (Google users see the same slots as before). Existing injectable-generator pattern (S01/S08/Q07) provides the LLM path. /api/meetings/book is already multi-provider + sovereign. Zero schema migration needed.
- **residual** Browser/runtime verification deferred (Playwright down per worklist line 11): UI flow works end-to-end, slots rendered real, books sovereign meeting, invite shows join link, proactive chip triggers correctly on meeting-request inbound. Deep-dock version (inbox_thread chat surface + CRM-cluster grounding + draft handoff + persisted history) is flagged not faked per worklist line 81. Rate-limiting strategy (quota per endpoint vs Inngest guard) is a config decision to verify post-build. Recipient timezone handling (propose in user's tz, optionally UTC) is a minor UX refinement; core path uses user's local tz."
- **evidence** meeting-booking.ts:19-29 (Google-only getAvailableSlots), calendar-write.ts:15-80 (multi-provider resolution order, CalDAV→Microsoft→Google), caldav.ts:118 (fetchCalDavMeetings exists), calendar-microsoft.ts:75 (getMicrosoftAccessToken exists), book/route.ts:150-175 (activity metadata shape, capacity enforcement), user-scope.ts:44 (getInboxScope pattern), conversations/detail/route.ts:32-35 (scope pattern), ask/route.ts (cited context pattern), writing-profile.ts:38 (buildWritingStylePrompt exists), video-meeting.ts (sovereign link generation), reply-handler.ts:131-140 (first-user bug), _PHASE2-WORKLIST.md:116 (residual explicitly stated)

### R09 — R09 — Plaintext & malformed-MIME graceful fallback
- **verdict** buildable · **storage** pure-compute · **completeness** 9/10 · **~120 LOC**
- **approach** R09 is 85% shipped: text path (linkify + fold + direction), sanitizer (2-layer), and robustness to malformed HTML are all in place and tested. The residuals are four isolatable tasks that complete the spec without schema migration. (1) Add empty-state UI when both html and text are falsy, showing "(no content)" in tertiary color; (2) wrap sanitizeEmailHtml() in try/catch at render, falling back to text on exception; (3) detect mis-typed HTML in plain text (heuristic: check for `<` followed by letter in text path, route through sanitizer); (4) change imap.ts line 114 from silent `continue` to degraded-capture (record headers + any decodable text, mark bodyHtml null).
- **files** app/(dashboard)/inbox/_email-body.tsx, lib/inbox/sanitize-email.ts, lib/inbox/linkify.ts, lib/inbox/email-fold.ts, lib/integrations/imap.ts, __tests__/imap-smtp-mailbox.test.ts
- **plan**
  - 1. Wrap sanitizeEmailHtml() in useMemo with try/catch guard in _email-body.tsx:30, return empty string on error to trigger text fallback
  - 2. Add empty-state branch to _email-body.tsx:110 rendering: if both html and text are falsy/empty, render div with '(no content)' in --color-text-tertiary
  - 3. Add mis-typed-HTML detector: regex /^\s*</m on text; if match, sanitize text as HTML (route through _email-body HTML path)
  - 4. In imap.ts line 114, replace `continue` with degraded capture: on simpleParser error, record available headers (from/to/subject/date) + null bodyHtml + empty body, push minimal SyncedEmail
  - 5. Add unit test to imap-smtp-mailbox.test.ts: malformed MIME yields recorded activity with headers but null html
  - 6. Add unit test to inbox-sanitize-email.test.ts or new email-body.test.ts: empty text+html renders '(no content)', sanitizer exception falls back to text
- **residual** Runtime verify: mis-typed-HTML detection heuristic (regex false-positives on plaintext like "Hello <name>") — caught by manual Playwright test (quoted in spec 41-42). Charset fallback for IMAP text decode (R10 residual, not R09 scope). Signature detection + earlier-message fold (R05 residual). Per-sender "always show images" memory (R02 residual, user_prefs JSONB).
- **evidence** _email-body.tsx:38 gate (no empty branch) | sanitize-email.ts:136 (no exception guard) | imap.ts:114-115 (silent `continue` drop) | linkify.ts:30 (pure, ready) | email-fold.ts:48 (pure, ready) | linkify.test.ts + fold.test.ts (both unit-tested) | sanitize.test.ts:84-88 (malformed robustness verified)

### C01 + G08 — Voice-matched grounded draft reply (agentic compose)
- **verdict** buildable · **storage** pure-compute + activities.metadata JSONB (voice samples cached) + user_preferences JSONB (optional P03 flag when built) · **completeness** 9/10 · **~1200 LOC**
- **approach** C01 and G08 are a single bundled feature: C01 owns the agentic compose flow (voice match + tone match + Insert/Send); G08 is the GTM-grounding context provider (cited bundle: deal/signal/last-interaction/role). They share one endpoint `POST /api/inbox/draft-reply` backed by `lib/inbox/draft-context.ts`. The implementation reuses existing patterns: writing-profile.ts (voice samples), call-mode/prospect-brief.ts (cited brief assembler), ask-thread.ts + summarize-thread.ts (injectable LLM generators with citations), getInboxScope (tenant/user scope), and the _conversation-pane.tsx preparedDraft + openReply + handleSent infrastructure. No schema migration required; voice samples and draft context live in request-time compute or are cached in JSONB. Zero-retention (P03) is deferred; the feature must fail-closed when absent.
- **files** lib/inbox/draft-context.ts, app/api/inbox/draft-reply/route.ts, lib/inbox/__tests__/draft-context.test.ts, app/(dashboard)/inbox/_conversation-pane.tsx, lib/writing-profile.ts
- **plan**
  - 1. lib/inbox/draft-context.ts — Assemble cited GTM bundle: compose call-mode brief + last-interaction + freshness-gated signals + role-status + G07 classification (if available). Every fact carries a citation; uncitable facts → [à compléter] placeholder. Fail-soft: grounding error → empty-but-flagged bundle, never blocks. Test: draft-context.test.ts — known/unknown/stale-signal/obsolete-role/multi-deal cases.
  - 2. lib/inbox/draft-reply-generator.ts — Injectable compose helper: `draftReply(context, voiceSamples, messages, generate?)` defaults to traced Anthropic/OpenAI (claude-sonnet-4-6) but accepts deterministic generate in tests. Prompt = thread + buildWritingStylePrompt(samples) + cited brief. Schema: {subject, body, citations[], explanation, placeholders[]}. Fail-closed: empty string on error. Test: 4 unit tests (voice-present, voice-absent, uncitable-fact, grounding-error).
  - 3. app/api/inbox/draft-reply/route.ts — POST endpoint: body={conversationKey}, scope via getInboxScope, load conversation by key, fetch voice samples + draft context, call draftReply generator, return {draft, explanation}. Rate-limit check (checkRateLimit('llm')), 401 unauth, 404 unknown thread, 500 on LLM failure → empty draft. Test: route test (known thread with deal, unknown sender, zero samples, grounding error).
  - 4. _conversation-pane.tsx integration — 'Draft reply' button next to existing **Reply** button (reuse openReply signal path). Inline card above composer: body in Inter 13px, citations as superscript chips (reuse G01 popover), explanation in secondary 12px. Buttons: **Insert draft** (Sparkles icon) pre-fills setComposer, **Send** routes through existing send + handleSent. No auto-send, ever. Test: pane interaction test (draft inserts, edits, sends; nothing auto-sends).
  - 5. Scope + retention + edge cases — getInboxScope blocks cross-tenant key lookups (return 404). P03 flag check: if enabled AND supported (pending build), don't persist draft; explanation notes "AI processing off". Unknown sender → draft answers thread only, explanation offers 'Add to CRM' (G02 link). Multiple open deals → ground in most-relevant, list rest in explanation. Stale signal/role excluded by freshness + role-status SSOTs. Test: scope/retention/edge-case test.
  - 6. Verify + ship — tsc clean, vitest green on all 5 test suites, browser verify (draft inserts/edits/sends), push as single commit with co-author trailer.
- **blockers** None — all dependencies exist. P03 (zero-retention flag storage) is deferred; C01/G08 must check for P03 and gracefully degrade (no persistent cache, explanation notes it).
- **residual** Deep dock-pinned inbox_thread chat surface (spec Q07 follow-up: CRM-cluster grounding + draft handoff + persisted history) = runtime-verify follow-up, not faked. P03 (tenant aiProcessingProfile + retention headers gating LLM calls) = pending build, C01/G08 must fail-closed without it. Multi-tenant letter-case or special-char edge cases in contact matching = accept as known future hardening. Playwright browser verification deferred per PHASE2-WORKLIST line 11 (runtime verify deferred). Browser-time draft flow interaction testing = deferred to Playwright harness when available."
- **evidence** - C01 spec line 110-118: voice seam exists at `lib/writing-profile.ts:10` (getWritingSamples), `:38` (buildWritingStylePrompt); reused by `app/api/emails/route.ts:132` and `lib/chat/tools/action.ts:97`. ✓
- C01 spec line 112-113: reply generator exists at `app/api/emails/suggest-reply/route.ts` (3 tones, claude-sonnet-4-6, tracedGenerateObject, rate-limited). ✓
- C01 spec line 114-115: composer seam exists: `_conversation-pane.tsx:91` (setComposer), `:147-188` (openReply with preparedDraft), `:186` (handleSent). ✓
- C01 spec line 116-117: Call Mode brief exists at `lib/call-mode/prospect-brief.ts:177` (getProspectBrief), jsonb cache, fail-closed validateBriefTexts. ✓
- G08 spec line 110-112: Freshness SSOT at `lib/signals/freshness.ts:80` (isSignalFresh), `:100` (filterFreshSignals); last-interaction at `lib/accounts/last-interaction.ts:36`; role-status at `lib/contacts/role-status.ts`. ✓
- Phase2 worklist line 59-62: S01+S08 summarize-thread.ts shipped with injectable pattern. ✓
- Phase2 worklist line 63-65: C04 rewrite.ts shipped with injectable pattern (rewrite.ts:43-59 shows lazy import + defaultGenerate). ✓
- Phase2 worklist line 74-81: Q07 ask-thread.ts shipped with injectable + citations (ask-thread.ts:55-61 buildAskThreadPrompt). ✓
- _conversation-pane.tsx:150-162: preparedDraft + openReply infrastructure ready to accept grounded draft. ✓
- Draft-context.ts does not exist: `find . -name draft-context.ts` returns zero results. ✗
- Draft-reply route does not exist: `find . -path "*/api/inbox/draft-reply/route.ts"` returns zero results. ✗

### C10 — C10 — Scheduling-email drafter (real open slots, sovereign visio)
- **verdict** buildable · **storage** pure-compute · **completeness** 9/10 · **~650 LOC**
- **approach** Build a POST /api/inbox/propose-times endpoint (like the existing C07/C04/S08 routes at app/api/inbox/compose/draft) that mirrors the injectable-generator pattern from rewrite.ts and ask-thread.ts. The core logic (propose-times.ts) is pure + unit-testable: compose real available slots from getAvailableSlots (which queries Google freebusy at meeting-booking.ts:19), voice-match them using buildWritingStylePrompt (writing-profile.ts), inject a sovereign Jitsi link via createSovereignMeeting (video-meeting.ts), and return a draft + slots[] + citations. The route scopes via getInboxScope (user-scope.ts) and rate-limits. Slots themselves are ephemeral (zero-retention per P03 — never persisted beyond the request). UI adds a "Propose times" action alongside "Book meeting" in _conversation-pane.tsx (line 352, CalendarPlus icon already wired), opening a panel that calls the route, surfaces a draft + clickable slot chips, and on accept calls the existing /api/meetings/book (which already books + writes CRM + writes activities.metadata).
- **files** lib/inbox/propose-times.ts, app/api/inbox/propose-times/route.ts, app/(dashboard)/inbox/_propose-times-slot.tsx, __tests__/inbox-propose-times.test.ts, lib/integrations/meeting-booking.ts, lib/integrations/video-meeting.ts, lib/integrations/calendar-write.ts, lib/writing-profile.ts
- **plan**
  - 1. Extend getAvailableSlots (meeting-booking.ts:19) to support CalDAV/Microsoft free/busy so non-Google users get real slots — union busy across multiple accounts. Add tests for multi-provider conflict detection (available-slots.test.ts).
  - 2. Create lib/inbox/propose-times.ts: core pure logic (slotMs → Interval[] via freeSlots from availability.ts, format with timezone/recipient context, voice-phrase using buildWritingStylePrompt, inject sovereign link, return {body, slots[], citations}). Injectable generate param for tests. Fail-closed on any error (return empty draft + fallback copy).
  - 3. Create app/api/inbox/propose-times/route.ts: POST {conversationKey, durationMinutes?, count?, contactEmail?} → validates scope (getInboxScope), fetches real slots via getAvailableSlots, calls propose-times(), returns {draft, slots[], acceptTokens}. Rate-limit. No-calendar fallback = fallback copy + prompt to connect.
  - 4. Create app/(dashboard)/inbox/_propose-times-slot.tsx UI: slot chips (start/end/tz label), selectable (single or multi), each carries an accept token. Draft preview + "send later" button. Reuse composer/toast patterns.
  - 5. Wire _propose-times-slot into _conversation-pane.tsx: add "Propose times" button next to "Book meeting" (both use CalendarPlus icon, near line 352), open panel on click, fetch from /api/inbox/propose-times, call onAcceptSlot → /api/meetings/book with {contactId, startTime, meetingType, conferencing}.
  - 6. Slot freshness: before send/accept, re-fetch via getAvailableSlots to verify the slot is still free. Surface "slot taken, re-proposing" if collision. No persistence — slots live only in memory during the request.
  - 7. Tests: propose-times.test.ts (slot composition + voice + link injection + fallback), route.test.ts (auth + scope + rate-limit + no-calendar + acceptance), UI.test.ts (slot chips + accept flow).
- **residual** Residual for runtime verify: (1) Slot race (two prospects accept the same slot simultaneously) — first booking wins, second gets re-propose (handled by /api/meetings/book conflict detection, test offline). (2) Recipient timezone inference (spec §Design sketch) — can be added as a future refinement; fallback = propose in user's tz + label it. (3) Playbook-triggered / deep-dive auto-drafts (spec §Requirements, C03 residual) — the C03 draft-stage-consume backend exists but proactive triggers are not wired; C10 only delivers the on-demand \"Propose times\" action, not the auto-draft. (4) CalDAV/Microsoft extension (Task 1): requires vendor API testing (tsdav for CalDAV, MS Graph for Outlook freebusy) — verify real free/busy before shipping. (5) Browser integration test (MeetingSchedulerCard open/close, slot accept flow) — Playwright deferred per worklist, verify UI/UX end-to-end after this build.
- **evidence** meeting-booking.ts:19 exports getAvailableSlots (Google-only, no CalDAV/Microsoft); video-meeting.ts:141 exports createSovereignMeeting (Jitsi injection, no Meet/Teams); calendar-write.ts:96 exports bookSovereignMeeting (already wired to /api/meetings/book route.ts:43, capacity-aware, CRM write); writing-profile.ts:38 exports buildWritingStylePrompt (reusable for voice-matching); user-scope.ts:44 exports getInboxScope (scope enforcement pattern); _conversation-pane.tsx:14 imports CalendarPlus icon, line 33 imports MeetingSchedulerCard (existing \"Book meeting\" UI). ask-thread.ts:78–100 + rewrite.ts:43–62 demonstrate the injectable-generator pattern (lazy-load AI SDK, defaultGenerate + optional generator param for tests, fail-closed). ask-thread.ts route at app/api/inbox/conversations/ask/route.ts is the reference pattern (scope + read-only + stateless). No propose-times endpoint exists (grep -r returns 0 matches for propose-times/propose-slots/scheduling-email-draft). CAL03 at worklist line 116 notes the booking backend + UI exist (✓); residual = voice-matched draft + multi-provider slots (this is C10).

### Q01 + Q02 + Q05 — Q01/Q02/Q05 — Semantic inbox + whole-inbox Ask-AI + cross-entity search
- **verdict** buildable · **storage** user_preferences JSONB (for saved searches in Q01 residual); pure compute for Q01/Q02/Q05 core (reuse searchHybrid + existing chat tools + context-graph infra; no new schema) · **completeness** 9/10 · **~2200 LOC**
- **approach** Q01, Q02, Q05 form a coherent stack: (1) inbox search (Q01) reuses searchHybrid + scopeConversationRows to scope mail to the user's mailbox post-hoc (embeddings table has no mailbox_id), (2) Q02 reuses the existing chat dock + /api/chat agentic loop, applies getInboxScope wrappers to the inbox-specific retrieval tools to prevent cross-mailbox leakage, (3) Q05 lifts the graph ∥ hybrid merge pattern from chat/route.ts:466 into a new cross-entity search endpoint that groups hits by entity_type, applies mailbox scope to mail, applies freshness filters. All three are pure retrieval + app-layer scoping; no LLM generation in Q01, injectable generation in Q02 (delegated to chat), graph+vector in Q05. No schema migration needed — embeddings already hold all entity types.
- **files** lib/inbox/search-query.ts, lib/ai/embeddings.ts, lib/chat/tools/query.ts, lib/chat/surface-from-path.ts, lib/inbox/user-scope.ts, lib/inbox/ask-thread.ts, lib/ai/context-graph.ts, app/(dashboard)/inbox/page.tsx, app/api/inbox/conversations/route.ts, app/api/chat/route.ts, components/chat/chat-dock.tsx, NEW: app/api/inbox/search/route.ts, NEW: lib/inbox/cross-entity-search.ts, NEW: app/(dashboard)/inbox/_search-field.tsx
- **plan**
  - 1. Add embed-on-capture in email-capture.ts so inbound mail is indexed at sync, not only manually (verify freshly captured activity appears in embeddings)
  - 2. Implement GET /api/inbox/search?q=... — call searchHybrid(q, 40, tenantId), filter to entity_type='activity' emails + outbound, apply scopeConversationRows + getInboxScope, collapse to conversations via buildConversations, return [{conversationKey, snippet, score, matchedMessageId}]
  - 3. Wire search field + '/' hotkey in inbox header; skeleton + empty state; reuse _conversation-list.tsx row rendering with highlighted snippet + relevance dot
  - 4. Snippet highlighting + RRF-score relevance indicator (3-step dot, never fake %)
  - 5. Partial/degraded handling (one path down → fallback badge + retry hint)
  - 6. Add inbox surface prompt addendum in surface-from-path.ts so dock on /inbox prefers mail tools
  - 7. Wrap searchEmailsByMetadata + semanticSearchEmails in lib/chat/tools/query.ts with getInboxScope filters (mailbox scoping applied post-fetch, not in SQL)
  - 8. Verify inbox-thread links resolve (/inbox?conversation=<key>) + chat renderer handles them
  - 9. Eval fixture: inbox questions with known cited answers; assert no fabrication on empty retrieval
  - 10. Implement GET /api/inbox/search?scope=all for cross-entity: run searchContextGraph(q, tenantId) ∥ searchHybrid(q, tenantId) in parallel, group by entity_type, apply mailbox scope to mail hits, apply freshness (signal TTL + role guardrails), resolve lead-entity cluster (reuse Call Mode brief builders + last-interaction SSOT)
  - 11. UI: grouped results (People/Companies/Deals/Mail/Notes) with badges + deep links; show stale signals as labelled, never asserted
- **residual** 1. Saved searches (user_preferences JSONB map) — buildable but deferred; Q01 ships with transient search only. 2. Q02 deeper vision (pin global chat dock to inbox_thread surface, hand draft to composer, persist thread-scoped history) is runtime-verify follow-up, not faked. 3. Q05 "lead entity cluster resolution" reuses Call Mode patterns but needs verification that last-interaction SSOT + brief builders are accessible/composable in the search context. 4. Freshness integration (signal TTL + role guardrails) — the module (lib/signals/freshness.ts) exists but needs integration test to verify suppression rules apply correctly in the grouped render. 5. Partial/degraded UI (marking one path down) is skeleton-ready but needs manual test that the banner renders when vector or full-text fails. 6. Browser/runtime verification (Playwright down per worklist line 11) deferred — the endpoints are unit-testable, UI is component-testable, but end-to-end search/ask flows need live app.
- **evidence** Q01 spec (line 14–17): \"Our inbox today has **no search box at all** — `/inbox` only reads a `?conversation=` URL param (`inbox/page.tsx:70`). We already own the retrieval engine (pgvector + BM25 + RRF over an `embeddings` table, `lib/ai/embeddings.ts`)\"; inbox/page.tsx:70 is now line 103–107 (deep-link handling). searchHybrid exists at embeddings.ts:236. Q02 spec (line 126): \"GAP: `/inbox` resolves to a *global* surface (`surface-from-path.ts:74`) with no mailbox-scope on the chat tools\"; surface-from-path.ts:74 is now line 74 (GLOBAL_LABELS inbox entry). semanticSearchEmails exists at query.ts:763, searchEmailsByMetadata at query.ts:388. Q05 spec (line 72): \"searchContextGraph(q, tenantId) ∥ searchHybrid(q, tenantId)\" pattern is cited as existing in chat/route.ts:466 (not yet read but referenced in embeddings.ts header). ask-thread.ts:105–125 shows injectable generation pattern. getInboxScope is at user-scope.ts:44–67; scopeConversationRows at user-scope.ts:113–128. inboundBelongsToUser/outboundBelongsToUser scoping helpers exist at user-scope.ts:100–104,94–98. /api/inbox/conversations route is fully implemented at conversations/route.ts, calling scopeConversationRows + loadConversationRows. Chat dock mounted at chat-dock.tsx; /api/chat route exists. context-graph.ts exists and provides entity extraction + graph traversal.

### N01 + N02 + N03 — Smart notifications + morning/EOD digest + focus mode cluster
- **verdict** buildable · **storage** user_preferences JSONB (preferences.inbox_reply, preferences.digest, preferences.focus, preferences.sla_breach) + activities.metadata {deferredByFocus} for focus-queued notifications · **completeness** 8.5/10 · **~2800 LOC**
- **approach** Build as a cohesive cluster in order N03 → N01 → N02: (1) N03 focus-mode gate that wraps sendNotification dispatch + persists to user_preferences.focus JSONB + tags deferred rows with metadata.deferredByFocus; (2) N01 notify-gate.ts pure logic that checks threshold+lane+automated before dispatch, integrated into capture/enrich path + idempotent on conversation key; (3) N02 event-driven sweep + builders (morning/EOD) that reuse up-next endpoints for sections, render via existing renderBrandedEmail, and record idempotency via user+date+kind key. All three reuse sendNotification 3-channel + notification_preferences, add new enum values to notificationTypeEnum, and run via cost-safe Inngest patterns (no per-tenant fans).
- **files** db/schema/outbound.ts, lib/emails/notifications.ts, lib/inbox/notify-gate.ts, lib/inbox/digest.ts, components/notification-bell.tsx, app/api/inbox/focus.ts, app/api/inbox/focus/summary.ts, app/(dashboard)/inbox/page.tsx, app/(dashboard)/inbox/_command-palette.tsx, inngest/inbox-digest-sweep.ts, app/api/inbox/digest/today.ts, lib/inbox/user-scope.ts
- **plan**
  - Step 1: Extend db/schema/outbound.ts notificationTypeEnum to add 'inbox_reply', 'digest', 'sla_breach' values (+ migration); add focus/sla_breach shapes to NotificationType union in lib/emails/notifications.ts:12–22.
  - Step 2: Implement lib/inbox/notify-gate.ts shouldNotify({importance, lane, threshold, isAutomated, lastInboundTs}) returning {notify, whyLine}; unit tests verify importance gate + handled lane exclusion + automated suppression.
  - Step 3: Extend sendNotification to accept optional {deferredByFocus, metadata} params; add focus.active check before email/Slack delivery; tag deferred rows with metadata.deferredByFocus=true for later summary.
  - Step 4: Add 'inbox_reply', 'digest', 'sla_breach' entries to components/notification-bell.tsx typeIconMap with lucide icons + token colors (Reply/Sunrise/Moon/Clock).
  - Step 5: Implement lib/inbox/digest.ts buildMorningBrief(scopeBundle) and buildEndOfDayWrap(scopeBundle) pure functions; consume up-next builders; return {sections:[{title, items:[{text, href, source}]}]}
  - Step 6: Create app/api/inbox/focus.ts POST handler to write preferences.focus JSONB (active, until, schedule, allowSlaBreach); add GET /api/inbox/focus/summary to fetch queued notifications and return one capped summary.
  - Step 7: Inngest inbox-digest-sweep every 30min: select users whose local morning/wrap time falls in window → inngest.send per-user inbox/digest-requested event; consumer builds + delivers via sendNotification + email + records idempotency row.
  - Step 8: Add GET /api/inbox/digest/today route returning same structure for in-app display (scoped via getInboxScope).
  - Step 9: Update inbox page.tsx to add focus toggle in header, show active indicator banner, hide empty lanes when focused, wire dedup on conversation key + lastInboundTs for idempotent notify dispatch.
  - Step 10: Add focus + digest + inbox_reply preference round-trip tests; integration tests verify focus suppresses live, queues, and flushes; digest idempotency; notify gate over-threshold at importance + under-threshold mutes.
- **residual** T04 importance persisted in metadata (currently computed read-time only) — N01 + N02 degrade to recency order until persisted. N01 bulk coalescing logic cap not implemented (residual complexity). N03 "allow SLA breaches" override paired with N04 (N04 not yet built, residual). Browser/E2E runtime verify deferred per Phase 2 worklist — no interactive Playwright tests; Slack webhook test assumes tenant settings + will silently fail (non-critical). Focus schedule auto-activation logic (evaluate in digest-sweep) works but daylight-saving tz edge case needs runtime verification. Dedup on conversation key assumes unique key stability across capture/enrich; if rekey happens, dedup may fail (rare, noted as edge case). Mobile responsiveness (N05) out of scope. Idempotency relies on deterministic keys (user+date+kind for digest, conversation key+lastInboundTs for notify); clock skew or key mutation risks double-send (acceptable under cost discipline)."
- **evidence** NotificationType union (no inbox_reply, digest, sla_breach yet): lib/emails/notifications.ts:12–22. notificationTypeEnum (same 10 types, needs extension): db/schema/outbound.ts:408–419. sendNotification 3-channel dispatcher (email + Slack + in-app): lib/emails/notifications.ts:34–148. notification_preferences schema with JSONB preferences column: db/schema/outbound.ts:425–437. typeIconMap (missing inbox_reply, digest, sla_breach): components/notification-bell.tsx:38–49. Importance scoring (T04 shipped): lib/inbox/importance.ts:52–85 scoreImportance(), used in conversations.ts:414–426. SLA core (N04 shipped): lib/inbox/sla.ts:24–31 checkSla(). HANDLED_LABELS + inboundIsAutomated (conversation lane assignment): lib/inbox/conversations.ts:170, 289–290. Sweep pattern (auto-briefing-trigger): inngest/auto-briefing-trigger.ts cron 0 * * * * + per-entity event dispatch. User-scope access: lib/inbox/user-scope.ts + app/api/home/up-next route:91 getInboxScope scoping. Preference storage pattern (lane-store/filter-store/seen-store): lib/inbox/{lane,filter,seen}-store.ts all via user_preferences JSONB k-v with no migration. Notifications read endpoint: app/api/notifications/route.ts GET + POST mark-read.

### CAL02 + CAL04 — Calendar features: book-from-email (CAL02) + RSVP (CAL04)
- **verdict** buildable · **storage** activities.metadata JSONB via clobber-safe || merge; injectable-LLM for time extraction (claude-sonnet-4-6 via tracedGenerateObject, lazy import() pattern, fail-closed) · **completeness** 8.5/10 · **~950 LOC**
- **approach** CAL02 extracts proposed meeting times from the latest inbound email using a quarantined LLM prompt (claude-sonnet-4-6 via injectable tracedGenerateObject), presents candidates to the user for confirmation, then pre-fills the existing POST /api/meetings/book flow with the selected time/duration—reusing all existing booking, calendar-write, and sovereign-visio infrastructure. CAL04 builds iTIP REPLY/COUNTER methods in ics.ts (sibling to buildIcs), wires a POST /api/inbox/rsvp endpoint that routes via the user's calendar provider (CalDAV/Graph/Google/SMTP), optional Mark Done on accept, and renders RSVP controls on the R12 invite card—both features scope to the user's mailbox/calendar with zero-retention of raw extraction data beyond the booking transaction.
- **files** lib/inbox/proposed-time.ts, lib/inbox/proposed-time.test.ts, lib/integrations/ics-reply.ts, lib/integrations/ics-reply.test.ts, app/api/inbox/proposed-time/route.ts, app/api/inbox/rsvp/route.ts, app/(dashboard)/inbox/_conversation-pane.tsx, lib/inbox/user-scope.ts
- **plan**
  - 1. Create lib/inbox/proposed-time.ts: LLM-driven time extractor (injectable generator, quarantined prompt via wrapUntrustedInput, schema { startIso, durationMinutes, rawPhrase, confidence }, fail-closed, reuse Claude Sonnet 4.6 pattern from summarize-thread.ts)
  - 2. Write lib/inbox/proposed-time.test.ts: unit tests covering 'Thursday 3pm' → ISO, relative dates (tomorrow, next Tue), multiple candidates, none detected, injection attempts, edge cases (past time, ambiguity)
  - 3. Create app/api/inbox/proposed-time/route.ts: GET route accepting conversationKey, calls loadConversationRows (existing), resolveConversationRows (existing), fetches latest inbound from conversation.messages, runs proposed-time extractor, returns candidates + timezone; scope via getInboxScope(tenantId, userId)
  - 4. Create lib/integrations/ics-reply.ts: buildIcsReply(invitation, decision, proposeAt?) → iTIP REPLY with PARTSTAT (ACCEPTED/TENTATIVE/DECLINED) + SEQUENCE + organiser; buildIcsCounter(invitation, proposeAt) → COUNTER method; reuse ics.ts utilities (toIcsUtc, foldIcsLine, escapeIcsText)
  - 5. Write lib/integrations/ics-reply.test.ts: unit tests for valid REPLY/COUNTER generation, real calendar client acceptance, sequence bumps, recurring vs single-instance
  - 6. Create app/api/inbox/rsvp/route.ts: POST { conversationKey, decision, proposeAt? } → build iTIP via ics-reply, send via the user's provider (findSmtpMailbox → CalDAV/SMTP; getMicrosoftAccessToken → Graph; getCalendarClient → Google), write event if applicable, optional Mark Done (check user_preferences for setting), scope via getInboxScope, return success + status
  - 7. Update _conversation-pane.tsx: detect R12 invite card presence (residual—R12 not yet built, so this is the wiring surface); add RSVP button row (Accept/Tentative/Decline + Propose new time) beneath invite; use lucide icons (Check/CircleHelp/X/Calendar), semantic token colors (-success/-warning/-error), keyboard bindings (y/m/n when invite focused)
  - 8. Verify lib/inbox/user-scope.ts has getInboxScope + scopeConversationRows; ensure both CAL02 and CAL04 routes call getInboxScope to enforce single-user mailbox isolation
- **blockers** R12 (inline .ics render + RSVP card surface) is NOT YET BUILT (_PHASE2-WORKLIST.md:137 flagged [ ]). CAL04's UI surface depends on R12. The back-end RSVP API (step 6) is buildable immediately, but step 7 (UI wiring) is residual until R12 lands or a mock invite card is scaffolded for testing. Recommended: build steps 1-6 (CAL02 + CAL04 back-end) now; defer step 7 UI to R12 integration or mark as runtime-verify with Playwright integration test once R12 appears.
- **residual** R12 inline .ics render + invite card (INBOX-CAL04 UI surface): depends on parsing text/calendar MIME parts + rendering a first-class invite card in the pane. Once R12 ships, wire RSVP buttons to it. Until then, RSVP endpoint is ready, UI test is deferred. CAL02 back-end (time extraction + booking) is self-contained and buildable. CAL02 UI test (browser: "Book this" button appears, pre-fills, books) requires Playwright verification of the pane button + dialog flow. Capacity gate, conflict-check, tz resolution, zero-retention are all handled by reusing bookSovereignMeeting + existing free/busy logic—covered by INBOX-CAL02 acceptance criteria but browser verify deferred (Playwright down per PHASE2-WORKLIST.md:11).
- **evidence** INBOX-CAL02.md:26-27 (booking reuses POST /api/meetings/book, bookSovereignMeeting); INBOX-CAL02.md:85-87 (API: GET /api/inbox/proposed-time → candidates[]); INBOX-CAL02.md:94-96 (AI via tracedGenerateObject, quarantined, confidence); INBOX-CAL04.md:17-25 (RSVP sends iTIP REPLY, updates calendar, optional Mark Done); INBOX-CAL04.md:57-58 (API POST /api/inbox/rsvp, metadata cached on activity); _PHASE2-WORKLIST.md:133 (CAL02/CAL04 = reuse meeting scheduler + ics); calendar-write.ts:96-162 (bookSovereignMeeting signature, mailbox resolution, capacity gate preserved); ics.ts:89-132 (buildIcs, toIcsUtc, escapeIcsText, foldIcsLine utilities); activities table (core.ts: metadata jsonb field, supports meeting_scheduled); _conversation-pane.tsx:441-447 (MeetingSchedulerCard pattern, detail.contact scoping); summarize-thread.ts:59-79 (injectable generator pattern: lazy import(), defaultGenerate, fail-closed); conversations.ts:256 (lastInbound message available in detail endpoint); user-scope.ts (enforces single-user mailbox scope)

### C12 — INBOX-C12 — Inline grammar / autocorrect
- **verdict** buildable · **storage** user_preferences JSONB (personal_dictionary key, resource "inbox") + activities.metadata JSONB for optional grammar history · **completeness** 8.5/10 · **~550 LOC**
- **approach** Build a lightweight, client-side typo corrector + personal dictionary (no schema migration, uses existing user_preferences JSONB storage pattern from filter-store.ts / seen-store.ts). Reuse injectable-generator pattern from rewrite.ts/translate.ts for optional language-aware grammar suggestions (deterministic dictionary run on hot path, LLM grammar checks debounced and optional). Integrate inline into _conversation-pane.tsx via a new _grammar-inline.tsx component with wavy underlines and accept/dismiss popover. Language detection already exists in lib/infra/language.ts; seed personal dictionary from user's company/product vocabulary at first run. Default toggle is on; persist via user_preferences JSONB (no migration).
- **files** lib/inbox/autocorrect.ts, lib/inbox/autocorrect-engine.ts, lib/inbox/personal-dictionary-store.ts, app/api/inbox/compose/grammar/route.ts, app/(dashboard)/inbox/_grammar-inline.tsx, __tests__/inbox-autocorrect.test.ts
- **plan**
  - 1. Create lib/inbox/autocorrect-engine.ts — high-confidence typo map (teh→the, doubled words, sentence casing), URL/email/quote/code region skipping, undo-safe (character positions + reversible ops). Unit tests: teh→the reversible, URL untouched, doubled-word fix.
  - 2. Create lib/inbox/personal-dictionary-store.ts — per-user personal_dictionary in user_preferences (resource 'inbox', key 'personalDictionary'; reuse filter-store.ts pattern). Seed from company vocabulary (CRM + Pilae/Elevay brand names) on first read. GET/POST endpoints for 'add to dictionary' + 'keep original' via user_preferences.
  - 3. Create lib/inbox/grammar-check.ts — injectable-generator pattern (buildGrammarPrompt + defaultGenerate + grammar(body, language, generate?) factory). Language detection via detectLanguage(text) from lib/infra/language.ts. Optional LLM grammar flags (FR/EN rules, elision, agreement); fail-closed on error. Debounce on client; server call optional.
  - 4. Create app/api/inbox/compose/grammar/route.ts — POST /api/inbox/compose/grammar {body, language?} → {corrections[{text,type,suggestion}], grammar[{start,end,message,rule,suggestion}]}. Stateless, read-only, scope-checked.
  - 5. Add _grammar-inline.tsx component in app/(dashboard)/inbox/ — inline corrections UI (wavy underlines, popover with suggest/dismiss/add-to-dictionary). Integrate into _conversation-pane.tsx composer. Detect language from body on keystroke.
  - 6. Wire toggle into EmailComposerPanel — per-user settings (user_preferences key 'grammarEnabled', default true). IME detection (suppress during input). Paste detection (flag only, no silent rewrite).
  - 7. Tests: inbox-autocorrect.test.ts (high-confidence typo + region skip + undo), inbox-personal-dictionary.test.ts (store/seed), inbox-grammar-check.test.ts (language detection, injectable generator, fail-closed). All deterministic; LLM tests mock generate param.
- **blockers** None. O06 (autonomy hub) is optional — C12 can ship with a simple on/off toggle in settings (defer the promotion dial). Schema migration not required (user_preferences JSONB is already available and used for seen-store/filter-store). IME/paste handling is client-side only.
- **residual** LLM-driven "Fix grammar" (full rewrite, not inline flag) is residual — that's C04. Runtime verify deferred: (a) actual grammar correction quality in edge languages (FR elision, German capitalization) requires interaction with real LLM. (b) User feedback on false-positive underlines (toggle frequency, sensitivity tuning) — can be dialed via user_preferences later. (c) IME input safety (Emoji, CJK, Arabic input) — browser APIs (CompositionEvent) can detect, but real-world corruption risk needs manual testing with actual IME input. Flagged not faked if issues arise.
- **evidence** INBOX-C12.md:lines 1-89 (full spec); lib/inbox/rewrite.ts:lines 1-77 (injectable-generator pattern, fail-closed, uninjected defaultGenerate); lib/inbox/translate.ts:lines 1-68 (same pattern, language-aware); lib/inbox/seen-store.ts:lines 1-38 (JSONB user_preferences storage pattern — resource+key schema); lib/inbox/filter-store.ts:lines 1-37 (same JSONB pattern); lib/infra/language.ts:lines 1-66 (detectLanguage already implemented, 12 language markers); autonomy.ts:lines 1-57 (per-rule dial pattern, though C12 can start with simple on/off); _PHASE2-WORKLIST.md:line 72 (C12 unchecked, listed as 'may fold into C04'); _EXECUTION-LOG.md:lines 49,88,119,138 (C12 listed as LLM-residual, part of T4 composing features).

### P03 — P03 — AI data handling & opt-out (zero-retention)
- **verdict** buildable · **storage** user_preferences JSONB (per-user settings) + tenants.settings JSONB (per-tenant ai_processing_profile) + activities.metadata JSONB (trace provider/residency/retention fields) · **completeness** 8/10 · **~550 LOC**
- **approach** P03 is implementable NOW because tenants.settings JSONB already exists as an extension point (proven by 50+ TenantSettings keys), llm_calls table already captures traces fire-and-forget, and all inbox AI features already use injectable defaultGenerate patterns for testing isolation. The barrier is not schema — it's gating logic + configuration plumbing. Gate inline at the three AI route entry points (summarize/ask/rewrite) by reading the tenant profile + calling getModelForTask() early to fail closed if profile=off. Extend llmCall to record getActiveProvider() output into activities.metadata (alongside existing cost/latency), making the provider residency auditable per call. The settings UI card reuses the exact admin-gated endpoint pattern already proven in /api/settings/privacy/route.ts.
- **files** lib/config/tenant-settings.ts, lib/ai/llm-call.ts, lib/ai/ai-provider.ts, app/api/inbox/conversations/summarize/route.ts, app/api/inbox/conversations/ask/route.ts, app/api/inbox/compose/rewrite/route.ts, app/api/settings/ai-profile/route.ts, app/(dashboard)/settings/privacy/page.tsx
- **plan**
  - 1. Extend TenantSettings interface in lib/config/tenant-settings.ts: add aiProcessingProfile: 'standard'|'sovereign'|'off' + noTraining: boolean, with defaults (standard, false). Add 'anthropic'|'mistral' provider info fields in dpaStatus for residency tracking.
  - 2. Extend LlmCallTrace in lib/ai/llm-call.ts to include provider, residency, retentionPolicy fields; modify persistCall to write getActiveProvider() + isAnthropicEuConfigured() + retention flag into the trace metadata (activities.metadata JSONB merge, not overwrite).
  - 3. Add checkAiProfileGate(tenantId, surface) helper in lib/ai/ai-provider.ts to read tenant profile from getTenantSettings and return {allowed:boolean, reason:string}; also add helper to gate Mistral-only profiles (throw if LLM_PROVIDER!=mistral but profile=sovereign).
  - 4. Gate the three inbox AI routes: summarize/route.ts, ask/route.ts, rewrite/route.ts — each calls checkAiProfileGate(authCtx.tenantId, 'inbox-summary'|'inbox-ask'|'inbox-rewrite') after auth, returns 403 if profile=off.
  - 5. Create app/api/settings/ai-profile/route.ts: GET returns {aiProcessingProfile, noTraining, providerInfo}; PUT (admin-gated via requireAdmin) merges into tenants.settings, replicating /api/settings/privacy pattern exactly.
  - 6. Add 'AI processing' card to app/(dashboard)/settings/privacy/page.tsx following the existing card idiom (Card + CardBody + icon + title + description + Badge for EU/residency status); three-way radio (Standard EU / Sovereign / Off) + 'No training' toggle; call PUT /api/settings/ai-profile on change, show success toast.
  - 7. Add unit tests: tenant-settings test for new keys + defaults; ai-provider test for checkAiProfileGate(profile=off/sovereign/standard); route tests for each API (mock getTenantSettings + verify 403 on off, verify provider routing on sovereign). Verify tsc --noEmit passes.
- **blockers** None architectural. The injectable AI pattern (defaultGenerate + tracedGenerateObject) already exists in summarize-thread.ts:78-103, rewrite.ts:43-62, ask-thread.ts:78-103 — no refactor needed. Mistral routing already exists (ai-provider.ts:160-179). The dpaStatus in TenantSettings already tracks DPA for anthropic (tenant-settings.ts:355-361), so provider residency is stateful. No live Inngest — routes are READ-ONLY on tenant config, so no async verification needed this session; browser/runtime verification (that off-profile actually blocks the UI) is deferred.
- **residual** 1. "AI details" affordance (popover showing what was sent + model + retention) on each AI output in the inbox pane — requires wiring a small popover component into _conversation-pane.tsx and fetching the trace via GET /api/inbox/conversations/{key}/ai-details (to expose the trace row + redacted content scope). Runtime browser verify needed. 2. Verify email-derived embeddings purge on contact deletion — search inngest/data-retention.ts or the contact-deletion path for the embedding-store cleanup; if missing, add a deletion pass over the vector/embedding tables for that contact's tenant scope. Needs DB/vector-store runtime verify. 3. Sovereign fail-closed when MISTRAL_API_KEY unset — already gated in ai-provider.ts:176-179, but the UI should surface "Sovereign not provisioned" on the settings card when LLM_PROVIDER!=mistral; getActiveProvider() tells us the active state. 4. Zero-retention header/flag transmission to the model (req.headers['anthropic-no-training'] or mistral equivalent) — the AI SDK may not expose this yet; verify provider SDK docs; if not, flag for post-P03 as a provider-integration task (falls under 'prod-covered' if already negotiated with Anthropic/Mistral).
- **evidence** TenantSettings type at lib/config/tenant-settings.ts:9-362 (no aiProcessingProfile present). llmCalls schema at lib/ai/ai-observability.ts:26-70 (model/outcome/metadata present, provider/residency absent). llmCall persistCall at lib/ai/llm-call.ts:137-165 writes tenantId/model/outcome but no provider. getActiveProvider() already exists at ai-provider.ts:269-276. Inbox AI routes at app/api/inbox/conversations/summarize/route.ts:15-46, ask/route.ts (no profile gating). /api/settings/privacy pattern at app/api/settings/privacy/route.ts:26-52 (admin check, PUT updates tenants.settings). Tenant profile can store JSONB: tenants table has settings column (core.ts), and getTenantSettings/updateTenantSettings already proven pattern.

### G02 — G02 — Auto-capture to CRM (approval-gated, human-in-the-loop)
- **verdict** buildable · **storage** user_preferences JSONB (for "always show images" per sender, future); activities.metadata JSONB (clobber-safe via || merge for link-safety warnings per-link) · **completeness** 8/10 · **~280 LOC**
- **approach** The capture backend (captureInboundEmail, recordCapturedActivity, listPendingApprovals, approveCapture, rejectCapture) is already complete in lib/capture/approval.ts and lib/capture/email-capture.ts; the spec says only the inbox UI surface is missing. Build four serverless route handlers (GET /api/inbox/captures/pending, POST /api/inbox/captures/:id/{approve,reject}) reusing the existing backend, tenant-scoped via getInboxScope. Then add a "Review captures (N)" header chip in the inbox page that opens a review drawer component showing pending captures with approve/reject buttons, and render "Captured by Elevay" as a tertiary-colored provenance line on the pane. Use strict zero-provider naming, design-token colors (--color-bg-card, --shadow-floating, lucide icons), and test-validate that review-mode email → pending count → approve → activity created.
- **files** app/api/inbox/captures/pending/route.ts, app/api/inbox/captures/[id]/approve/route.ts, app/api/inbox/captures/[id]/reject/route.ts, app/(dashboard)/inbox/_capture-review-drawer.tsx, app/(dashboard)/inbox/_provenance-line.tsx, lib/capture/capture-ui-helpers.ts
- **plan**
  - 1. Create GET /api/inbox/captures/pending route: call getInboxScope to narrow to user's mailbox(es), then listPendingApprovals scoped to tenant, return count + rows with proposedActivity (don't insert yet).
  - 2. Create POST /api/inbox/captures/:id/approve route: tenant+mailbox scope guard, call approveCapture, return inserted activityId and refresh pending count.
  - 3. Create POST /api/inbox/captures/:id/reject route: tenant+mailbox scope guard, call rejectCapture, return success and refresh pending count.
  - 4. Build _capture-review-drawer.tsx: float-right card with lucide Inbox icon, list pending captures (From → subject → summary), approve/reject buttons per row (lucide Check/X), close on escape, keyboard shortcut 'g then r'.
  - 5. Add 'Review captures (N)' header chip to page.tsx conversation-list section, click opens drawer, only renders when N>0.
  - 6. Build _provenance-line.tsx: render below sender email in pane header, text='Captured by Elevay', color var(--color-text-tertiary), size [11px], no provider name anywhere.
  - 7. Unit-test route handlers for tenant/mailbox scope (verify user can't see other tenant's captures), approve flow (pending→activity created), and reject flow (pending→rejected, no activity).
  - 8. Integration test: ingest inbound in review mode, verify GET pending returns 1, POST approve inserts activity, verify activity appears on contact timeline.
- **residual** Runtime verify deferred (no Playwright): browser rendering of pending count, approve removes it, provenance line displays. Deep dock-pinned version (inbox_thread chat surface + CRM-cluster grounding + draft handoff + persisted Q&A history) is out of scope per worklist (residual follow-up, flagged not faked)."Add to CRM" CTA for unknown senders in the G01 sidebar is out of scope for this tranche (will be a future UI junction with captureInboundEmail + contactCreationMode honor). Multi-mailbox attribution (pending captures must show which mailbox they arrived at) is covered by the existing getInboxScope + metadata.to guard but needs E2E verification."
- **evidence** Capture backend complete: lib/capture/approval.ts:80-181 (recordCapturedActivity, listPendingApprovals, approveCapture, rejectCapture all exist and tested). Schema: db/schema/core.ts:270-290 (captureApprovals table with tenantId, kind, sourceRef, proposedActivity, status, reviewedByUserId). lib/capture/email-capture.ts:184 (captureInboundEmail routes through recordCapturedActivity). Inbox scoping pattern: lib/inbox/user-scope.ts:44-68 (getInboxScope returns user's mailbox addresses; used in detail route app/api/inbox/conversations/detail/route.ts:32). No inbox-facing captures endpoint exists (Glob shows no app/api/inbox/captures directory). No review drawer UI exists (grep returns no 'Review captures' or 'Captured by' text). The spec explicitly states (INBOX-G02.md line 116-118): 'No inbox-facing pending-captures endpoint or review drawer exists yet (grep: none under app/api/inbox/captures). The capture engine is complete; only the inbox surface is missing.'

### G12 — Voice-of-customer rollup across threads (inbox source + enrichment wiring + reports surface)
- **verdict** buildable · **storage** user_preferences JSONB (autonomy dial per T11) + activities.metadata JSONB (threadId already present) + injectable-LLM (optional ARR estimation) · **completeness** 8/10 · **~450 LOC**
- **approach** The VoC engine and classifier already exist (shipped). The build adds inbox as a source by: (1) extending POST /api/customer-requests schema to accept source:'inbox' (add to enum at line 26); (2) wiring the classifier into the email enrichment pass in sync-functions.ts after sentiment/intent analysis, running classifyCustomerMessage on inbound body and writing customer_requests rows with source:'inbox' + verbatim quote (trimmed 2000 chars) + canonicalKey + metadata{threadId, accountId, contactId}; (3) creating GET /api/voice-of-customer?source=inbox aggregation endpoint (tenant-scoped, deduped by canonicalKey, sorted by ARR weight); (4) adding "Voix du marché" rollup UI to /reports with inline citations (MessageSquareQuote icon, expandable cards, ARR chip, status). The autonomy dial (T11) gates extraction via Suggest/Auto, the matched quote is the "why". ARR estimate is derived from deals linked via contact→company→deals (no LLM needed, just a join). No prod schema migration — all storage fits in existing JSONB + enrich pass wiring.
- **files** app/apps/web/src/lib/capture/email-capture.ts, app/apps/web/src/inngest/sync-functions.ts, app/apps/web/src/app/api/customer-requests/route.ts, app/apps/web/src/db/schema/voice-of-customer.ts, app/apps/web/src/lib/voice-of-customer/classifier.ts, app/apps/web/src/app/api/voice-of-customer/route.ts, app/apps/web/src/app/(dashboard)/reports/page.tsx, app/apps/web/src/lib/inbox/autonomy.ts, app/apps/web/src/lib/inbox/user-scope.ts
- **plan**
  - 1. Extend POST /api/customer-requests schema: add 'inbox' to source enum (line 26 of route.ts)
  - 2. Wire VoC classifier into inngest/sync-functions.ts enrichment pass (after sentiment/intent): classifyCustomerMessage on rawContent, write customer_requests rows for qualified inbounds with source:'inbox', capture verbatim (2000 char trim), canonicalKey, and metadata{threadId, contactId, companyId, accountId}
  - 3. Add contact→company→deals join helper to retrieve tenantArrUsd from linked account's deal value (nullable, fall through to count if missing)
  - 4. Ensure activities.metadata.threadId is already populated (verified line 380 of email-capture.ts; reuse)
  - 5. Create GET /api/voice-of-customer endpoint as new route (parallel to GET /api/customer-requests): aggregate by canonicalKey, sum ARR, count, list source threads, tenant-scope via getInboxScope
  - 6. Create source filter ?source=inbox for rollup endpoint
  - 7. Add 'Voix du marché' section to /reports/page.tsx: render rollup with expandable cards (canonicalKey title, kind badge, count chip, ARR weight chip, status badge), each card shows verbatim quotes as citations (MessageSquareQuote icon, click opens thread), drilled to /inbox?threadId=
  - 8. Wire autonomy dial (T11) via user_preferences JSONB, default Suggest for inbox extraction (resolveAutonomy logic already in autonomy.ts line 27)
  - 9. Add 1-2 integration tests: verify SSO ask creates one row when classified; verify logistics mail creates none; verify dedup on canonicalKey; verify ARR weight sorts correctly
  - 10. Add UI test: render rollup, click citation, verify thread opens (runtime verify deferred per worklist)
- **residual** Runtime browser verification (Playwright down per worklist) — click citation opens correct thread in current tenant. Deep metrics dashboard (deal-stage-derived ARR estimation vs simple deal.value fallback) — deferred to follow-up. Cross-tenant isolation tripwire tests — covered by T11 scope guards but flagged for DB-level RLS hardening (P05 residual). LLM-assisted nuanced dedup (e.g., 'SSO' vs 'SAML' as canonical synonyms) — regex dedup sufficient for MVP; future LLM judge wraps classifier. Saved/pinned VoC items in UI — covered by Customer Council admin surface, not needed for inbox rollup MVP."
- **evidence** VoC schema exists: db/schema/voice-of-customer.ts:23 with canonicalKey dedup (:41), tenantArrUsd (:44), source enum (:37 not yet including 'inbox'), metadata (:49). Classifier shipped: lib/voice-of-customer/classifier.ts:177 classifyCustomerMessage(text) → ClassifiedRequest|null. POST /api/customer-requests/route.ts:26 schema limits source to [chat, support, onboarding_feedback, in_product_widget] — 'inbox' must be added. Email capture already records threadId: lib/capture/email-capture.ts:377 threadId in activity, :380 threadId in metadata. Enrichment pass exists: inngest/sync-functions.ts has analyzeEmailBatch (sentiment/intent) — VoC classify must run same pass. Activities schema: db/schema/core.ts entityType/entityId link contact/company, which link to deals for ARR. Reports surface: app/(dashboard)/reports/page.tsx hosts cohort/revenue insights — rollup UI goes there. Autonomy dial: lib/inbox/autonomy.ts:27 resolveAutonomy() gate already implemented. getInboxScope: lib/inbox/user-scope.ts scopes all reads by tenant/user.

### P04 — Data residency / sovereign hosting (Pilae)
- **verdict** buildable · **storage** user_preferences JSONB k-v (residency_profile per tenant via tenants.settings JSONB) + activities.metadata JSONB for mail body residence proof + pure-read-time posture computation · **completeness** 8/10 · **~280 LOC**
- **approach** P04 decomposes into three independent, buildable layers: (1) Store the residency_profile on the tenant via tenants.settings JSONB k-v (pattern proven by lane-store.ts / filter-store.ts / seen-store.ts), with no migration required; (2) Compute the posture endpoint GET /api/inbox/residency at read time — inspect the tenant profile + environment (GDPR_REGION, DATABASE_URL, LLM_PROVIDER / MISTRAL_API_KEY, VIDEO_MEET_BASE_URL) and return honest mail/storage/AI/video residency facts + any remaining non-sovereign sub-processor names, gated by region config so false claims are impossible; (3) Wire a Settings card in the Privacy tab to render the posture (lucide icons Server/ShieldCheck/Globe/Video, no emoji, no vendor names, link to /sub-processors), matching the privacy-page card idiom at line 220/254. All three are READ-ONLY (no IMAP/SMTP/CalDAV changes, no Inngest), testable by stub-checking the env var logic, and the IMAP/SMTP/CalDAV infra (imap.ts verifyImap + connected_mailboxes schema + AES-256-GCM secret_encrypted) is already shipped and verified by the code.
- **files** lib/inbox/residency-posture.ts, app/api/inbox/residency/route.ts, app/(dashboard)/settings/residency/page.tsx, lib/inbox/__tests__/residency-posture.test.ts
- **plan**
  - 1. Create lib/inbox/residency-posture.ts: pure-function computeResidencyPosture(tenantProfile: {mailboxProvider, region}, env: {GDPR_REGION, DATABASE_URL, LLM_PROVIDER, MISTRAL_API_KEY, VIDEO_MEET_BASE_URL}) → {mail, storage, ai, video, subProcessors[]}, no side effects, testable by varying env inputs. Validate: GDPR_REGION=eu requires DATABASE_URL in EU region (check host for scaleway.com/infomaniak/ch/eu-central-1); Mistral API key gating for EU-sovereign AI claim; VIDEO_MEET_BASE_URL check for visio.pilae.ch; subProcessors list (Anthropic US on non-EU, Mistral when selected, any non-sovereign blob store if configured). Fail safely: if GDPR_REGION≠eu, return posture.storage='Default / not configured', no EU claim.
  - 2. Unit test lib/inbox/__tests__/residency-posture.test.ts: (a) EU sovereign scenario (GDPR_REGION=eu, DATABASE_URL=Scaleway Frankfurt, MISTRAL_API_KEY set, VIDEO_MEET_BASE_URL=visio.pilae.ch, mailbox=smtp_custom) → mail/storage/ai/video all sovereign + subProcessors empty or minimal; (b) Non-EU default (GDPR_REGION not set / =us) → posture.storage='Default', no sovereign claims, subProcessors=[Anthropic US, Neon US]; (c) Mixed tenant (some users Gmail, some Zimbra) → residency profile reflects weakest link (handled by caller via scope query); (d) Mistral unprovisioned on sovereign tenant → ai='Configured but unavailable' + subProcessors=[Anthropic US fallback]. 8 tests, tsc0.
  - 3. Create app/api/inbox/residency/route.ts: GET handler, getInboxScope + getInboxScope.tenantId to read tenant profile from database (initially from tenants.settings JSONB key 'residency_profile' or default to {mailboxProvider: 'inferred'}, later persisted by settings UI); call computeResidencyPosture; return JSON {mail, storage, ai, video, subProcessors[{name, exposure}], sourceRegion, claim_verified_at}. Owner-scoped (reuse getInboxScope pattern from ask-thread.ts route), read-only, fail-soft on missing config. Route tests: verify JWT auth, owner scope, posture shape.
  - 4. Unit test app/api/inbox/residency/route.ts: (a) EU tenant, authenticated → 200 {mail: 'self-hosted IMAP', storage: 'EU/CH', ai: 'EU-sovereign', video: 'sovereign', subProcessors: []}; (b) Non-auth → 401; (c) Unauthenticated call → read-only so request identity is NOT checked (just scope per caller). 4 tests, tsc0.
  - 5. Create app/(dashboard)/settings/residency/page.tsx: Client component (use-client), fetch GET /api/inbox/residency on mount, render a Card (card-bg-color token) with 4 rows: (a) Mail: lucide Server + 'Self-hosted IMAP' or 'Google/Outlook'; (b) Storage: lucide Globe + 'EU/CH resident' or 'Default / not configured'; (c) AI: lucide ShieldCheck + 'EU-sovereign (Mistral)' or 'US-based (Claude)'; (d) Video: lucide Video + 'Sovereign (Jitsi)' or 'Google Meet'; each row includes sub-label citing the source (e.g., '(GDPR_REGION=eu, Scaleway Frankfurt)' gray text, secondary token); a footer link to '/sub-processors' (text-secondary, lucide ExternalLink); loading state (skeleton) + error fallback ('Unable to load residency posture; check admin settings'). No emoji, no vendor brand names in the card itself (reserve vendor names + exposure to /sub-processors). Light/dark tokens from globals.css (--color-bg-card, --color-text-secondary, --color-success). Integrate into the existing privacy/page.tsx sidebar (stub at line 220 idiom; residency card = one of the tabs or auto-mounted below the DPA section). Do NOT rebuild the entire privacy page — just add the residency section.
  - 6. Stub the wiring in settings/privacy/page.tsx: import the Residency card or render it inline. Keep existing compliance/DPA logic untouched; the residency posture is READ-ONLY + orthogonal. tsc0, no new imports to privacy-page.tsx beyond what Residency card exports.
  - 7. Ensure connected_mailboxes query works: verify that getInboxScope.mailboxes includes provider (already does at connected_mailboxes schema line 230), so the posture endpoint can infer smtp_custom vs gmail vs outlook from the loaded mailboxes. If a tenant has mixed mailboxes (user A: Gmail, user B: Zimbra), the posture reflects 'Mixed (weakest link is Gmail)' or similar honest language. Residual: per-mailbox residency profile (multi-user scenario) is a follow-up; core feature is tenant-level posture.
  - 8. Verify tsc + vitest run the residency test suite with 100% green, no linting or formatter errors. Commit with both co-author trailers.
- **residual** Per-mailbox multi-user residency: if a tenant has user A on Gmail + user B on Zimbra, the posture should reflect per-user scope or highlight the mixed state honestly (currently tenant-level posture only; multi-user scenario deferred to a follow-up settings UI where each user configures their profile); Residency policy enforcement (e.g., reject US-based outbound if sovereign tenant selected) — that's P05 (tenant-isolation audit) + application logic across all send paths, out of scope here; CAL05 sovereign Jitsi video link injection already shipped (video-meeting.ts per log batch 21), so this endpoint just DECLARES the visio.pilae.ch config, verification of actual link injection is in CAL05; Attachment residency flagging (per P04 spec line 74 'Attachments storage must also reside in EU/CH') — R04 is ocean, so attachment storage strategy is unresolved; flag if blob store is non-sovereign but make no attempt to fix it; Sub-processor list completeness — this endpoint names Anthropic/Mistral/Neon/hosting (Vercel/Clever Cloud/Infomaniak), but the full list lives on /sub-processors page; keep this posture endpoint lean (just mail/storage/AI/video posture + primary sub-processors by category, not exhaustive); Runtime runtime verification (Playwright down) deferred — the endpoint is correct by construction (env-based logic, no DB writes, fail-soft); the UI card rendering needs browser verify but can be spot-checked by stub-fetch in a manual test after merge.
- **evidence** INBOX-P04.md lines 15-160 (full spec + current-state notes confirming smtp_custom/IMAP/CalDAV/AES-256-GCM infra exist); _CODEBASE-NOTES.md lines 59-77 (schema verification: connected_mailboxes has provider/imap_host/smtp_host/secret_encrypted/caldav_url); _PHASE2-WORKLIST.md line 113 '[~] P04 IMAP/SMTP/CalDAV + Mistral-EU + sovereign visio ✓ — residual: residency_profile (JSONB), posture endpoint, settings card'; lib/ai/ai-provider.ts lines 160-200 (Mistral EU provider + shouldUseMistral logic already built); db/schema/core.ts line 23 (tenants.settings JSONB already exists for config storage); db/schema/auth.ts lines 141-164 (user_preferences JSONB pattern for per-user settings, proven by lane-store.ts / filter-store.ts in Phase-2); app/(dashboard)/settings/privacy/page.tsx existing card structure at line 220 (Card/CardBody idiom proven); app/(legal)/security/page.tsx lines 218-240 (sovereign stack already documented in prod); db/schema/outbound.ts lines 230-247 (connected_mailboxes.provider includes smtp_custom, imap_host/port/smtp_host/port/secret_encrypted/caldav_url/caldav_last_sync_at all present); lib/integrations/imap.ts lines 1-75 (verifyImap + IMAP credentialing infra proven); _EXECUTION-LOG.md batch 21 line 161 'P04 residency posture' + 'residency_profile (JSONB), posture endpoint, settings card' confirms the exact residual remaining.

### P05 — P05 — Per-user isolation & tenant-scoping audit (close the inbox read-scope gap)
- **verdict** buildable · **storage** user_preferences JSONB, activities.metadata JSONB (for per-user/tenant scoping flags) · **completeness** 8/10 · **~250 LOC**
- **approach** P05 is a hardening task: the app-layer per-user scoping already exists (getInboxScope + scopeConversationRows in lib/inbox/user-scope.ts) and works in all inbox read paths. Current gap: inbox queries do NOT run inside withTenantTx (db/rls.ts:44), so RLS remains fallback-allow and cannot backstop a forgotten WHERE clause. Build plan wraps all 5 inbox read routes through withTenantTx, adds a tripwire to catch id-filters without tenantId, implements cross-tenant/cross-user e2e, then verifies inboundBelongsToUser null-handling (fail-closed). Strict RLS rollout for inbox tables is deferred (flagged as ocean dependency, must coordinate org-wide R-08b).
- **files** lib/inbox/user-scope.ts, lib/inbox/load.ts, app/api/inbox/conversations/route.ts, app/api/inbox/conversations/detail/route.ts, app/api/inbox/route.ts, app/api/inbox/conversations/ask/route.ts, app/api/home/up-next/route.ts, db/rls.ts, drizzle/0074_rls_enforced.sql, __tests__/rls.test.ts, lib/inbox/__tests__/user-scope.test.ts, __tests__/inbox-isolation.e2e.ts (new)
- **plan**
  - 1. Inventory: grep all inbox read paths (5 routes + ask-AI sidebar context queries) and verify each derives identity from getAuthContext() only, never request input. Document in e2e fixture. (lib/inbox/load.ts loads tenant-wide rows; scoping happens at assembly via scopeConversationRows in all 5 routes + ask-thread.ts)
  - 2. Add tripwire lint: extend __tests__/rls.test.ts with a source-tree scan that fails CI if an inbox query filters by (activities.id, outbound_emails.id, connected_mailboxes.id, inbox_triage.conversationKey) without AND-ing tenantId, or if an inbox route reads tenantId/userId/mailboxId from request input (headers/query/body).
  - 3. Wrap inbox reads in withTenantTx: update all 5 GET/POST routes (conversations, detail, outbound, ask, up-next) to call loadConversationRows inside withTenantTx(authCtx.tenantId, async (tx) => ...). Passes tx to loadConversationRows so queries use the bound context.
  - 4. Plumb tx through load.ts: update loadConversationRows to accept optional tx param; when provided, use it instead of global db for the 3 parallel queries (inbound, outbound, triage). Queries stay identical — the tx carries the RLS binding.
  - 5. Verify inboundBelongsToUser null-handling: unit-test that legacy rows with metadata.to=null return false (fail-closed, not shown to anyone). Extend user-scope.test.ts case to verify orphaned-inbound scenario.
  - 6. Cross-tenant + cross-user e2e: create __tests__/inbox-isolation.e2e.ts. Seed tenantA(userA + userA2) + tenantB(userB). Auth as A; hit all 5 inbox surfaces (list, detail, outbound, ask-this-thread, home/up-next) with forged ?mailbox=B and {tenantId:B} body params. Assert zero B rows, zero A2 rows ever returned. Plant a leak (add WHERE 1=1 to a query) and verify the e2e fails.
  - 7. Confirm ask-AI scope: verify ask-thread.ts + /api/inbox/conversations/ask inherit two-axis scope (getInboxScope before askThread call). Verify context queries take no identity param from the model. Unit test that a hallucinated foreign contactId in the thread yields zero context rows.
  - 8. Document shadow-mode lane: add comment to 0074 noting strict-RLS rollout for inbox tables is staged (shadow-mode logging first); that enablement is tracked as R-08b follow-up, not part of this build.
- **residual** Strict-RLS migration (dropping fallback clause from 0074 for inbox tables) is flagged as ocean: requires org-wide R-08b coordination and shadow-mode logging to avoid a sign-in-style outage. Email-derived embeddings + context-graph PII purge on deletion (per-tenant) is covered by INBOX-P03 task 6, cross-ref verified. Playwright e2e runtime verification is deferred (Playwright down); the tripwire + vitest e2e cover the gate. Deeper ask-AI vision (dock pin to thread, CRM-cluster grounding, persisted history) is P07 follow-up, not in this build scope."
- **evidence** INBOX-P05.md:18-21 (spec notes getInboxScope + scopeConversationRows); user-scope.ts:43-67 (getInboxScope); user-scope.ts:101-128 (scopeConversationRows + inboundBelongsToUser); conversations/route.ts:21-33 (inbox list derives identity from getAuthContext(), calls getInboxScope, scopes rows); detail/route.ts:21-34 (detail routes through getInboxScope + scopeConversationRows); route.ts (outbound) uses getInboxScope + scopeConversationRows; ask/route.ts:15-36 (ask-thread scopes via getInboxScope before askThread); up-next/route.ts:78-96 (up-next loads replies scoped by getInboxScope); rls.ts:44-54 (withTenantTx is THE only sound way to bind tenant context; no session-scoped set_config); load.ts:16-88 (loadConversationRows queries activities/outbound/triage WITHOUT withTenantTx, relying on app-layer WHERE tenant_id = ?); 0074_rls_enforced.sql:1-44 (RLS policies are fallback-allow, permit all when app.tenant_id IS NULL); rls.test.ts:79-111 (tripwire pattern: source-tree grep for session-scoped set_config); user-scope.test.ts:59-107 (unit tests for scoping, missing null-to-address case); P05 §6 "verify inboundBelongsToUser null-handling".

### C05 — C05 Autocomplete — grounded in history
- **verdict** buildable · **storage** user_preferences JSONB (toggle: "inbox"."autocomplete-enabled"; per-user, no migration) · **completeness** 8/10 · **~400 LOC**
- **approach** Build autocomplete as a stateless, debounced (250ms) LLM feature that grounds completions in the user's real sent-mail voice (via getWritingSamples from activities table) and the open thread's contact/deal context. No streaming needed — the completion is a single-shot ghost text overlay that accepts via Tab. Inject the LLM generator for deterministic tests (matching rewrite.ts/ask-thread.ts pattern). Rate-limit via rateLimitLLM. Toggle storage via user_preferences JSONB. Fail-closed: if LLM fails or fact-check fails, return empty completion.
- **files** lib/inbox/autocomplete.ts, app/api/inbox/compose/autocomplete/route.ts, lib/inbox/autocomplete.test.ts, components/email-composer-panel.tsx
- **plan**
  - 1. Create lib/inbox/autocomplete.ts with injectable-generator pattern: autocomplete(prefix, voiceSamples, context?, generate?) → {completion}. Build prompt grounding prefix in voice + context. Add stop rule: return empty if completion contains unverifiable fact (price/date/name not in voice/context). Test with deterministic generator.
  - 2. Create app/api/inbox/compose/autocomplete/route.ts (POST /api/inbox/compose/autocomplete): parse {prefix, conversationKey?}, fetch voice samples via getWritingSamples(tenantId), fetch thread context via conversations.detail route, call autocomplete lib, return {completion}. Rate-limit via rateLimitLLM. Scope via getInboxScope.
  - 3. Add ghost-text UI to email-composer-panel.tsx: debounce keystroke 250ms, call /api/inbox/compose/autocomplete, show completion in overlay span (color: var(--color-text-placeholder)), Tab accepts (insert + dismiss), Esc/typing cancels. Suppress during IME composition (use composition events).
  - 4. Add toggle storage: pattern from filter-store.ts, resource='inbox', key='autocomplete-enabled'. Wire toggle into settings UI (reuse O06 autonomy hub pattern). Default true. Disable requests if false.
  - 5. Write 5 unit tests: fact-check stop rule, unknown-deal fallback, debounce/cancel logic, voice grounding, IME suppression.
- **residual** Runtime browser verify (ghost text rendering, Tab/Esc UX, smooth typing feedback) deferred to interactive test. Per-user toggle UI (settings card in O06 hub) needs design review. Citation display (when completion cites CRM fact) optional refinement. Multi-language handling (C08 path) deferred. Deep grounding (deal-stage-matched next-step suggestions) can enhance later via C01 draft-context reuse.
- **evidence** C05 spec line 59-71 (design sketch + injectable pattern); rewrite.ts line 43-62 (injectable defaultGenerate + schema); ask-thread.ts line 78-103 (exact same pattern); filter-store.ts line 10-37 (user_preferences JSONB pattern); rate-limit.ts line 36-39 (rateLimitLLM proven); writing-profile.ts line 10-35 (getWritingSamples proven); email-composer-panel.tsx line 200-252 (controlled textarea, edit state, Escape handling). Worklist line 70 confirms C05 buildable if no streaming needed (verdict: no streaming needed, single-shot debounced completion).

### T11 + O06 — Per-feature autonomy dial + settings hub (T11/O06)
- **verdict** buildable · **storage** user_preferences JSONB (per-rule autonomy stored inline on filter/nudge/archive rules; non-rule capabilities in user_preferences.autonomy JSONB map) · **completeness** 7/10 · **~850 LOC**
- **approach** T11 and O06 form a unit: T11 is the per-rule autonomy mechanism (suggest/auto dial on each filter/nudge/archive rule with a never-auto-send guard); O06 is the unified hub that aggregates all per-rule and per-feature rungs into one audited dashboard. Since the autonomy.ts core is already shipped with deterministic logic (resolveAutonomy, defaultAutonomy, shouldPromote), the buildable work is: (1) add autonomy field to LabelFilter interface in filter-match.ts, add autonomy read/write to filter-store.ts and GET/PATCH handlers in filters/route.ts; (2) replicate for nudge config and archive rules in their respective stores; (3) build aggregator GET /api/inbox/autonomy listing all capabilities + rungs + track records; (4) build O06 hub page at /settings/inbox-autonomy with grouped controls, promoting offers, audit links, and pause-all. No prod schema migration — all autonomy data lives in user_preferences JSONB, leveraging the existing pattern (lane-store.ts, filter-store.ts).
- **files** lib/inbox/autonomy.ts, lib/inbox/filter-match.ts, lib/inbox/filter-store.ts, lib/inbox/lane-store.ts, lib/inbox/no-reply-nudge.ts, lib/inbox/archive-decision.ts, app/api/inbox/filters/route.ts, app/api/settings/autonomy/route.ts, app/(dashboard)/(rest)/settings/autonomy/page.tsx, __tests__/inbox-autonomy.test.ts, db/schema/auth.ts
- **plan**
  - 1. Add autonomy?: 'suggest' | 'auto' field to LabelFilter interface in lib/inbox/filter-match.ts:46; add to DeterministicFilter base type
  - 2. Update filter-store.ts to read/write autonomy as part of LabelFilter; set default to 'suggest' for new filters (per T11 requirement to earn trust)
  - 3. Update POST /api/inbox/filters/route.ts to accept autonomy param in filterSchema, default to 'suggest' (AI rules) or 'auto' (deterministic); update PATCH to allow autonomy flips
  - 4. Apply same autonomy field + store pattern to nudge config (lib/inbox/no-reply-nudge.ts + nudge store) and archive rules (lib/inbox/archive-decision.ts + archive store); add to their respective API routes
  - 5. Build GET /api/inbox/autonomy aggregator: query all per-user filters/nudges/archives + their autonomy fields; add user_preferences.autonomy JSONB for non-rule capabilities (reading/writing); join with mock audit track records (mock: {accepted: count, dismissed: count, undone: count} per rule); return grouped by capability area
  - 6. Build /settings/inbox-autonomy page: register in settings-sidebar.tsx under Workspace section with SlidersHorizontal icon; create grouped Card UI (Reading, Writing, Triage, GTM) with one-line description + Off/Suggest/Auto segmented control per capability; surface acceptance-rate badge and 'View activity' link to audit; add prominent 'Pause all autonomy' button that batch-sets all Auto→Suggest
  - 7. Wire approve/dismiss/undo endpoints per T11 spec (POST /api/inbox/suggestions/{id}/{approve|dismiss}, POST /api/inbox/rule-actions/{id}/undo) — mock/flagged for residual runtime verify (suggestion staging + audit logging)
  - 8. Add safety tests: verifyNeverAutoSend guard (triage rules never send), verifyPromotionLogic (shouldPromote at 20 accepted), verifyTenantScope (B can't see A's dials), verifyDefaultsSuggest (AI rules suggest, deterministic auto)
- **blockers** None. Autonomy.ts core is shipped + tested. Filter/nudge/archive stores already use user_preferences JSONB pattern (no migration needed). Per-rule rung writes go through existing PATCH endpoints. Non-rule rung storage reuses user_preferences (same pattern). The never-auto-send guard + promotion logic are deterministic + already proven. Suggestion approval/undo + audit table are residual (flagged, not faked — browser/Inngest runtime verify later).
- **residual** Suggestion staging on conversation metadata + approval/dismiss/undo endpoints (mocked with accept-counts for testing, real audit table + Inngest triggering = runtime verify). Per-rule action audit table (inbox_rule_actions: rule_id, user_id, conversation_key, action, rationale, taken_at, outcome — can be stored as conversations.metadata.rulesApplied JSONB if migration avoided, verified at runtime). Promotion offer rendering in O06 hub (gated until audit track record exists). Browser verify: inline suggestion cards in list/pane + keyboard shortcut behavior. Per-user/tenant scoping verified by getInboxScope pattern (code exists; functional test needed).
- **evidence** autonomy.ts:27-56 (core logic shipped, tested); autonomy.test.ts:1-40 (6 passing tests); filter-match.ts:45-50 (LabelFilter struct, no autonomy field yet); filter-store.ts:1-38 (user_preferences pattern for filters, no autonomy handling); filters/route.ts:19-25 (schema, no autonomy); autonomy.ts:12-13 (Autonomy type + RuleAction enum ship-ready); settings-sidebar.tsx:68-90 (Workspace section; ready flag available for gating O06 until T11 ships)

### P06 — Citations & provenance everywhere (wire CitedClaim into pane/_thread-summary/_thread-ask)
- **verdict** buildable · **storage** pure-compute + activities.metadata JSONB (for origin→"via Elevay" mapping server-side) · **completeness** 7/10 · **~450 LOC**
- **approach** P06 is the provenance backbone for all inbox AI surfaces. The ConfidenceState primitive already exists with the 4-state ladder (verified|likely|uncertain|unverified) and isVisibleAtDefault() gating. CitationChip (recording offset) and CitedText (marker splicing) exist for coaching; generalize these to inbox sources (message index → message click-through). Create a shared CitedClaim wrapper composing ConfidenceState + a generalized citation chip, then wire it into _thread-summary (summary header with confidence + citations), _thread-ask (answer + citations), and _conversation-pane entity/badge renderers. Update the response contracts (ThreadSummary, ThreadAnswer, sidebar facts) to carry {text, sources[], confidence}, with server-side origin→"via Elevay" mapping so no vendor names leak to the client.
- **files** components/ai-ui/confidence-state.tsx, components/coaching/citation-chip.tsx, components/coaching/cited-text.tsx, components/ai-ui/cited-claim.tsx, app/(dashboard)/inbox/_thread-summary.tsx, app/(dashboard)/inbox/_thread-ask.tsx, app/(dashboard)/inbox/_conversation-pane.tsx, app/(dashboard)/inbox/_prospect-brief.tsx, lib/inbox/summarize-thread.ts, lib/inbox/ask-thread.ts, app/api/inbox/conversations/summarize/route.ts, app/api/inbox/conversations/ask/route.ts
- **plan**
  - Create components/ai-ui/cited-claim.tsx: CitedClaim wrapper composing ConfidenceState + generalized citation chip (accepts messageIndex or null for non-message sources). Re-export ConfidenceState + citation helpers.
  - Extend lib/inbox/summarize-thread.ts: update ThreadSummary interface to carry {tldr: {text, confidence, sources[]}, keyPoints: Array<{text, confidence, sources[]}>} alongside citations[]. Update prompt to ask for confidence level per claim.
  - Extend lib/inbox/ask-thread.ts: update ThreadAnswer to carry {answer: {text, confidence, sources[]}, citations[]} alongside answered flag.
  - Update app/api/inbox/conversations/summarize/route.ts: map origin tags to 'via Elevay' server-side before JSON response.
  - Update app/api/inbox/conversations/ask/route.ts: same origin→'via Elevay' mapping.
  - Update _thread-summary.tsx: render TL;DR with CitedClaim wrapper (verify isVisibleAtDefault gates uncertain/unverified); render key points with per-point confidence chips; link citations to message indices.
  - Update _thread-ask.tsx: render answer with CitedClaim wrapper + click-through to cited messages; gate uncertain claims behind explicit reveal.
  - Update _conversation-pane.tsx entity chips + action-items rendering: wire CitedClaim for each extracted entity/action with source message link.
  - Update _prospect-brief.tsx: label enriched facts as 'via Elevay' (not vendor name) where applicable.
  - Add tests: verify CitedClaim renders verified/likely/uncertain/unverified correctly; isVisibleAtDefault gates low-confidence; citation click-through targets correct message; no vendor name in response payload; origin mapping works server-side.
- **residual** 
- HEAD-check on AI-produced URLs feeding unverified state (deferred to runtime verify, needs integration with HTTP layer)
- Freshness integration for signals + role-status (lib/signals/freshness.ts + lib/contacts/role-status.ts exist but not yet wired into CitedClaim)
- Dock-pinned deep Q&A with CRM-cluster grounding (INBOX-Q07 follow-up tracked separately, not in scope)
- Per-message assignee provenance (X01, tracked separately)
- Browser/Playwright verification that click-through citations actually navigate to source messages (deferred, Playwright down per worklist)
- Vendor-name lint tests (assertion that no known vendor strings reach UI) requires a canonical list of vendor names to filter
- Multi-tenant citation isolation edge case verification (citations resolve only within viewer's tenant/mailbox per INBOX-P05)

- **evidence** 
- ConfidenceState exists: C:/Users/ombel/leads-wt-inbox/app/apps/web/src/components/ai-ui/confidence-state.tsx:14-22 (4-state ladder), :28-52 (token palette), :101 (isVisibleAtDefault)
- CitationChip exists: C:/Users/ombel/leads-wt-inbox/app/apps/web/src/components/coaching/citation-chip.tsx:23-63 (recording offset click-through pattern)
- CitedText exists: C:/Users/ombel/leads-wt-inbox/app/apps/web/src/components/coaching/cited-text.tsx:20-56 (marker splicing)
- _thread-summary.tsx current rendering: C:/Users/ombel/leads-wt-inbox/app/apps/web/src/app/(dashboard)/inbox/_thread-summary.tsx:68-86 (renders tldr+keyPoints+citations, NO confidence chips yet)
- _thread-ask.tsx current rendering: C:/Users/ombel/leads-wt-inbox/app/apps/web/src/app/(dashboard)/inbox/_thread-ask.tsx:84-102 (renders answer with manual citation list, NO confidence wrapper)
- ThreadSummary contract: C:/Users/ombel/leads-wt-inbox/app/apps/web/src/lib/inbox/summarize-thread.ts:22-27 ({tldr, keyPoints[], citations[]}, NO confidence/sources per claim)
- ThreadAnswer contract: C:/Users/ombel/leads-wt-inbox/app/apps/web/src/lib/inbox/ask-thread.ts:31-38 ({answer, citations[], answered}, NO confidence/sources)
- Phase 2 worklist line 115: "P06 CitedClaim/SourceLink/Confidence components exist — residual LAKE: wire into pane/_thread-summary/_thread-ask + {text,sources,confidence} contract"
- Entity rendering in _conversation-pane.tsx:522-530 (key details badges have NO confidence/sources)


## flag-decision (2)

### X03 + X04 + X05 + X06 — Collaboration cluster: presence, labels, snippets, handoff (depends X01/X02)
- **verdict** flag-decision · **storage** user_preferences JSONB (X03 heartbeat + X04 label scope + X05 snippet vars) + activities.metadata JSONB (X03/X06 presence/handoff records) + inbox_presence table (TTL-swept presence keyed by tenant/conversation/user) + inbox_label/inbox_label_application (X04) + inbox_snippet/inbox_ai_prompt (X05) · **completeness** 8/10 · **~4200 LOC**
- **approach** X03–X06 form a tight collaboration cluster (presence, labels, snippets, handoff) all grounded in the same member+activity+JSONB infrastructure. They are buildable NOW via pure JSONB storage (no migration) + injectable AI (proven pattern in rewrite.ts), BUT they depend on X01 (assignment infrastructure) and X02 (comment/notify spine) shipping first. The specs are explicit: reuse X02's mention parsing + notification fan-out, reuse the JSONB k-v pattern from filter/lane stores, and gate writes via viewer-guard.ts. All four can ship in one 5–6-day sprint AFTER X01/X02 land; they share routes, schema extends, and UI patterns. However, Martin must decide: should X03–X06 be sequenced after X01/X02, or should we build X01/X02 first to unblock this cluster?
- **files** db/schema/outbound.ts (add inbox_presence table), db/schema/core.ts (add activity_type 'note', 'handoff' to enum; verify 'internal' metadata flag pattern), lib/inbox/mentions.ts (X02 reference; core already exists), lib/inbox/user-scope.ts (scope enforcement; exists), lib/auth/viewer-guard.ts (write-gate; exists), lib/auth/auth-utils.ts (requireAdmin; exists), lib/inbox/filter-store.ts (JSONB pattern reference), lib/inbox/lane-store.ts (JSONB pattern reference), lib/inbox/rewrite.ts (injectable-generator reference), lib/inbox/summarize-thread.ts (injectable-generator reference), app/(dashboard)/inbox/_conversation-pane.tsx (UI anchor for presence chips, assign, notes), app/(dashboard)/inbox/_conversation-list.tsx (row rendering), app/api/inbox/route.ts (root handler), app/api/inbox/conversations/route.ts (extend with label joins + handoff metadata), app/api/inbox/conversations/detail/route.ts (extend with presence, labels, handoff note)
- **plan**
  - 0. DECISION: Confirm X01 (assignment) and X02 (comments/notify spine) are in-flight or will be done before X03–X06 build starts. X03–X06 hard-depend on both.
  - 1. (X03 + X06 + core schema) Add inbox_presence ephemeral table (TTL-swept, tenant+conversation+user key); extend activities enum with 'note', 'handoff' types; add 'internal' flag to activities.metadata pattern.
  - 2. (X03) Presence heartbeat routes: POST /api/inbox/presence (upsert, member+, shared-mailbox-scoped, viewer-blocked), GET /api/inbox/presence (fresh rows, TTL sweep on read).
  - 3. (X06) Handoff route: POST /api/inbox/handoff (reassign + optional internal note, role-gated via viewer-guard, collision pre-check via lib/collision/).
  - 4. (X04) Label schema: inbox_label (tenant, normalized_name unique), inbox_label_application (tenant+conversation+label unique); CRUD routes (member+ create, admin rename/delete, viewer-blocked).
  - 5. (X04) Extend conversations route: join label_application, add ?label= filter lane, scope done archive (inbox_triage.status='done') team-visible via X01's shared-mailbox scope.
  - 6. (X05) Snippet schema: inbox_snippet, inbox_ai_prompt (both tenant, normalized_name unique); CRUD routes (author+admin governance, viewer-blocked).
  - 7. (X05) Variable resolver: {{var}} substitution from INBOX-G01 context (contact/company/deal) with explicit-marker fallback; reuse lane-store pattern for storing resolved vars server-side.
  - 8. (X05 + X04) Composer wiring: snippet picker (`;` shortcut, reuse Button/MoreMenu), AI-prompt menu (routed through C04 rewrite path, approval-gated, never auto-send).
  - 9. (X03 + X06 + X04) UI in pane + list: presence chips (Eye/PenLine icons, viewing/drafting state), assignee chip (from X01), label chips (Badge + --color-badge-* tokens, reuse), handoff banner (UserPlus icon, handed-off-by/when).
  - 10. (X03) Presence suppress on personal mailboxes (check scope: if conversation not team-visible, hide chips).
  - 11. (All) Permission tests: viewer POST → 403 (all create/apply/assign routes); cross-tenant rejection; role-freshness in X05 variables.
  - 12. (All) Unit tests: X03 presence TTL freshness, X04 label dedup by normalized name, X05 variable resolution with missing fields, X06 handoff collision notice + note internal-only guard.
  - 13. (Runtime verify deferred — Playwright pending): presence in two browser tabs (chips update on heartbeat), label lane filters + archive, snippet insertion resolves vars, share draft never sends, handoff notifies new owner.
- **residual** X01/X02 must ship first (hard dependency). Runtime verify deferred (Playwright pending): cross-browser presence heartbeat sync, label lane pagination for large archives, snippet variable resolution with stale CRM data (role-freshness), handoff notification delivery + collision notice rendering. AI-prompt scope (rewrite vs compose) + grounding to deal stage (GTM-aware prompts) requires C04 integration verify. "Share draft" internal-comment path must be structurally separate from send path (see INBOX-X06.md:76 best-in-class bar) — a code-review gate, not runtime verify."
- **evidence** file:C:/Users/ombel/leads/_specs/ai-native-inbox/_PHASE2-WORKLIST.md:119-120 (X01/X02 marked [M] MISSING), file:C:/Users/ombel/leads/_specs/ai-native-inbox/_PHASE2-WORKLIST.md:131-132 (X03–X06 grouped, reuse X02 + stores), file:C:/Users/ombel/leads/_specs/ai-native-inbox/INBOX-X03.md:120 (X03 depends X01+X02), file:C:/Users/ombel/leads/_specs/ai-native-inbox/INBOX-X06.md:88 (X06 reuses X02 comment spine), file:C:/Users/ombel/leads-wt-inbox/app/apps/web/src/lib/inbox/filter-store.ts:1-45 (user_preferences JSONB pattern proven), file:C:/Users/ombel/leads-wt-inbox/app/apps/web/src/lib/inbox/lane-store.ts:1-45 (JSONB pattern for personal state), file:C:/Users/ombel/leads-wt-inbox/app/apps/web/src/lib/inbox/rewrite.ts:43-77 (injectable-generator pattern, fail-closed), file:C:/Users/ombel/leads-wt-inbox/app/apps/web/src/lib/inbox/summarize-thread.ts:59-80 (lazy-loaded AI SDK in default generate), file:C:/Users/ombel/leads-wt-inbox/app/apps/web/src/db/schema/core.ts:activities table + activityTypeEnum (flexibility for note/handoff), file:C:/Users/ombel/leads-wt-inbox/app/apps/web/src/db/schema/outbound.ts:inbox_triage exists (team-visible done archive via X01 scope), file:C:/Users/ombel/leads-wt-inbox/app/apps/web/src/lib/auth/viewer-guard.ts:37-46 (write-gate pattern, SAFE_METHODS + allowlist), file:C:/Users/ombel/leads-wt-inbox/app/apps/web/src/lib/auth/auth-utils.ts:109-114 (requireAdmin pattern), file:C:/Users/ombel/leads-wt-inbox/app/apps/web/src/lib/inbox/mentions.ts:1-50 (@mention parsing core for X02 spine reuse)

### Q06 + Q08 — AI-native inbox Q06 (find-file) + Q08 (web-grounded) triage
- **verdict** flag-decision · **storage** Q06: blob-bytes (attachments, R04 ocean). Q08: user_preferences JSONB (webGroundingEnabled flag only; tool is stateless fetch) · **completeness** 8/10 · **~450 LOC**
- **approach** Q06 is blocked on R04 (blob attachment storage) — email-capture.ts:378-404 stores NO attachment refs; search-match.ts:6 flags it as ocean. Q08 gated web tool can build on existing infra (tenant-settings JSONB + ssrf-guard + orchestrator), but spec honestly notes (lines 127-130) sovereign backend is ocean. Recommend: ship hosted zero-retention path first (buildable lake), flag sovereign backend as INBOX-P04 follow-up. Q08 meets the buildable bar if scoped to hosted-only: tsc-clean, vitest-testable, SSRF-safe, no prod migration, injectable LLM calls via traced-ai pattern.
- **files** lib/config/tenant-settings.ts, lib/chat/tools/query.ts, lib/agents/orchestrator.ts, lib/chat/tool-router.ts, lib/infra/ssrf-guard.ts
- **plan**
  - (Q08 only, hosted path) Add webGroundingEnabled + residency_profile to tenant-settings.ts TenantSettings interface (lines 9-362)
  - (Q08) Implement webLookup tool in lib/chat/tools/query.ts: gated on webGroundingEnabled, { query: string } input, fetch via ssrf-guard, return { answer, sources:[{title,url,fetchedAt}] }
  - (Q08) Register webLookup in orchestrator.ts SPECIALIST_TOOL_GROUPS[research/data] and TOOL_GROUP_MAP
  - (Q08) Route fresh-fact intents (is this current/latest/just...) in orchestrator.ts routing logic
  - (Q08) Add external citation rendering in _thread-ask.tsx: distinct style vs via Elevay (Globe icon + tertiary text + as of <date>)
  - (Q08) Add rate/cost caps via rate-limit.ts pattern; zero-retention assertion (no mail in egress payload)
  - (Q08) Tests: web-lookup-tool.test.ts (gating, SSRF block, content-free egress, failure-degrade)
  - FLAG Q06: Cannot proceed without R04. FLAG SOVEREIGN BACKEND: design + provisioning decision needed (INBOX-P04).
- **residual** Q06 residual until R04 lands: deep-dock pinned Q07 version (thread scoped chat surface + CRM cluster grounding + draft handoff + persisted history). Q08 residual: sovereign self-hosted backend design, provisioning, and multi-backend routing. Approval-gated capture of fetched facts (INBOX-G02/G04 signal reuse) is buildable but deferred to follow-up. Runtime verify: live web fetch (Playwright down per worklist), external citation rendering in browser, rate-limiting enforcement under load.
- **evidence** email-capture.ts:378-404 metadata structure (no attachment refs). search-match.ts:6 R04 ocean callout. PHASE2-WORKLIST.md:85 disposition. tenant-settings.ts:9-362 complete interface (no webGroundingEnabled today). ssrf-guard.ts:1-120 exists and ready to reuse. orchestrator.ts:43-66 specialist tool groups pattern established.

