Aucune migration DB (zero DDL) : la colonne `personalization_sources` (jsonb) existe déjà sur `sequence_drafts` (app/apps/web/src/db/schema/outbound.ts:149-152) — on la peuple au lieu de la laisser `[]`. Le `kind` étendu (funding/headcount/tech/signal/news) reste dans le jsonb libre, pas d'enum à migrer.

Coupling de déploiement à flaguer :
1. T2 (peuplement des claims) DOIT précéder en prod l'activation du gate freshness (T4) et a un effet immédiat sur le gate citations SEND existant (sequence-draft-to-outbound.ts:136) : dès que personalizationSources contient des URLs réelles, le gate fail-closed peut recaller des drafts dont une source est morte — comportement voulu mais nouveau côté volume. Surveiller le taux de recall stale_citation après déploiement (logger.warn sequence-draft-to-outbound.stale_citation).
2. Drafts legacy (créés avant P1-11) ont personalizationSources=[] : approve (T3) et send les laissent passer sans vérif (R3.3). Pas de backfill requis ; les nouveaux drafts héritent des claims automatiquement.
3. T4 lit intelligenceBriefs.generated_at/created_at — confirmer la colonne exacte en T0 avant déploiement (open question 2 du design). TTL 14j en constante (FACT_FRESHNESS_TTL_DAYS), pas de config tenant pour l'instant.
4. Surcoût tokens marginal : les claims sont émis dans l'appel generateObject déjà existant (sequence-generator.ts:90), aucun appel LLM supplémentaire. Aucun appel LLM dans les gates (HEAD réseau + comparaison de date uniquement).
5. Cache URL 7j partagé (url-verifier-cache.ts:22) : le gate approbation réutilise le cache du gate génération/send — pas de nouveau trafic réseau si déjà vérifié <7j. Pas de fuite tenant (URL = ressource publique, cache global).
6. Dataset/eval : pas de nouveau dataset. Vérifier que la suite eval:run (sequence-quality) reste verte — l'ajout de claims[] au schéma ne doit pas dégrader gradeSequenceQuality (lib/evals/sequence-quality.ts opère sur body, pas claims).