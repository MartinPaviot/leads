Aucune migration DB (réutilise la table `intelligenceBriefs` ; upsert et schéma inchangés). Aucun dataset à charger. Couplage déploiement : (1) feature gardée par env `RESEARCH_AGENT_ENABLED` — défaut OFF en prod ; activer SEULEMENT après l'éval T6 (le chemin déterministe `fetchAllSources` reste le comportement par défaut, régression zéro). (2) Coût LLM attendu ~4x le pipeline mono-call actuel — surveiller `llm_calls` (surfaceId/promptId `research-agent-brief`) et le gate budget `enforceLlmBudget` avant activation à grande échelle. (3) Dépendance amont P1-10 (`enrichApollo`) : l'outil n'est ajouté au ToolSet que si l'impl est fournie ; tant qu'absente, pas de stub. (4) Le câblage lecture P0-2 (`readCachedBrief`, `prospect-context.ts:179-193`) est inchangé — aucun redéploiement coordonné requis côté génération. (5) `RESEARCH_AGENT_ENABLED` à ajouter dans la config env (Vercel) avant d'activer.