MIGRATION (requise, bloquante) : ALTER TABLE intelligence_briefs ADD COLUMN IF NOT EXISTS firmographics jsonb, ADD COLUMN IF NOT EXISTS firmographic_provenance jsonb DEFAULT '[]'::jsonb. Additif, idempotent, aucun backfill. Le runner drizzle casse a idx 0012 (memoire) : appliquer via db:push sur leadsens-localdev (dev) et via le runner custom db:migrate:apply ; ne jamais auto-migrer la prod depuis une branche non mergee. ATTENTION couplage prod (memoire "Prod schema behind Drizzle") : tant que la migration n'est pas appliquee en prod, l'upsert sur firmographics/firmographic_provenance throw au runtime malgre un build vert — d'ou le try/catch isolant (T4) OU le flag BRIEF_FIRMOGRAPHICS off ; deployer le code SEULEMENT apres la migration prod, ou garder le garde-fou actif. BUDGET : 1 credit export Apollo par build de brief FROID (plan free 75 credits/mois, cf _specs/apollo-tools.md) ; 0 credit sur cache-hit (TTL 14j) — pas de cout recurrent par generation. DATASET : aucun ; les briefs existants rendent firmographics=null (degrade au comportement P0-2) et s'enrichissent au prochain refresh post-TTL. DEPENDANCES : APOLLO_API_KEY deja en env (apollo-client.ts:11), aucune nouvelle cle/provider. COUPLAGE AMONT : P1-11 (citations) consomme la provenance par-champ produite ici ; P1-9 (agent de recherche) reste independant et non requis (chemin agent-tool flague hors scope).